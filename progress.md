# 进度日志：融合下载器平台扩展

## 会话：2026-07-24（Bilibili 下载速度优化）

### 阶段 17：Bilibili 有界并行 Range 下载与速度验证
- **状态：** complete
- 执行的操作：
  - 读取当前 Bilibili、YouTube、统一调度、测试和三个状态文件，确认 Bilibili 与 YouTube 上层质量流程相似，但 Bilibili 的 `max_workers` 尚未作用于单视频传输。
  - 保留 Bilibili 现有 4 MiB 稳定块和备用 CDN，新增最多 8 路并行 Range、线程本地 Session 连接复用、随机位置写回与完整文件大小校验。
  - 每个分块验证 HTTP 206、`Content-Range` 起止位置、总大小和实际长度；主节点失败后轮换备用节点，并行不可用时删除不完整流并回退原串行稳定路径。
  - 串行回退补充防护：非首块收到 HTTP 200 全文件响应时拒绝继续追加，避免把完整文件重复写入造成伪完成品。
  - 新增 4 项 Bilibili 速度回归测试；19 项 Bilibili 专项测试和全项目 57 项测试全部通过，语法及 Git 空白检查通过。
  - 第一次真实验证被 1 秒外层工具时限终止；确认无遗留 Python 进程和临时目录后，改用可持续等待单元完成验证，没有机械重复短时限。
  - 使用系统临时目录真实下载 `BV1oHNv6kEzB`：视频 239.2 MiB/8.97 MiB/s，音频 30.2 MiB/6.88 MiB/s，总耗时 45.383 秒。
  - 最终成品 283,189,702 字节，格式 `30080+30280`，FFprobe 验证 1920×1080/25fps H.264 + AAC，临时目录自动清理。
  - 核对并发打包记录：15:41:39 的 EXE 早于 15:44:50 的最终 Bilibili 源码，因此保留其 YouTube 阶段 16 验收记录，但不将其误报为已包含本阶段提速。
  - 使用正式 `build_exe.bat` 在最终源码之后重新构建，生成 250,157,214 字节单文件 EXE，SHA-256 为 `103C26944FFCB480D066E0DC53F8F6DB8E865537F43AA931A93B3F725E708A9F`。
  - 归档确认包含 Bilibili/YouTube 下载器与解析器、`requests.adapters`、yt-dlp-ejs、Deno、FFmpeg 和 FFprobe；构建后的 57 项测试再次通过。
  - 最终 EXE 隐藏启动 12 秒产生两个可响应的单文件进程，均通过正常关闭退出，未使用强制终止，也没有遗留测试进程。

## 会话：2026-07-24（浏览器 Cookie 锁定反馈）

### 阶段 15：Windows 浏览器 Cookie 锁定实机诊断
- **状态：** complete
- 执行的操作：
  - 用户在完全关闭可见 Chrome/Edge 窗口后，Chrome 与 Edge 自动读取仍提示无法复制 Cookie 数据库，并反馈不知道 cookies.txt 从何获得。
  - 只读检查确认当前仍有 24 个 Chrome 后台进程，其中“独立翻译窗口 - 划词翻译”仍是可见 Chrome 应用；因此 Chrome Cookie 数据库尚未满足无人占用条件。
  - 当前无 `msedge.exe`，但有 13 个其他应用使用的 `msedgewebview2.exe`；未擅自终止任何浏览器或 WebView2 进程。
  - 用户选择不终止 Chrome 后台进程，改为学习导出 cookies.txt。
  - 核对 yt-dlp 官方 FAQ、YouTube extractor 文档和扩展商店，确认使用 `Get cookies.txt LOCALLY`、无痕唯一标签页、`robots.txt` 和 Netscape 格式的安全导出流程。
  - 用户导出后首次导入提示格式错误；仅检查桌面候选文件名、大小和第一行，确认正确文件为 `www.youtube.com_cookies.txt`，另一个 `cookie.txt` 是 0 字节空文件，未读取或输出任何 Cookie 内容。
  - 扩展已由用户主动安装并完成本地导出；用户选择正确文件后导入成功，助手未读取或输出 Cookie 内容。
  - 实机运行日志确认导入模式生效，目标视频解析为 `313+140`、3840×2160/30fps、VP9 + AAC，并开始 4 路、4 MiB 分段下载。
  - 只读监控发现临时视频流已预分配为 5,036,208,838 字节；虽然文件大小与修改时间不变，但 30 秒内进程继续写入约 159 MiB，确认任务仍在正常并行写入而非停滞。
  - 用户确认下载完成后复核输出：最终 MKV 为 5,243,020,624 字节，`.youtube-*` 临时目录已清理；独立 FFprobe 确认 3840×2160/29.97fps VP9 视频、AAC 音频和 12,683.366 秒时长。

### 阶段 16：YouTube 新手 Cookie 导出引导与自动导入
- **状态：** complete
- 执行的操作：
  - 用户要求软件面向无相关经验的人自动获取 Cookie 或提供含扩展下载链接的详细教程。
  - 确定最小安全方案：匿名模式保持默认；将“官方扩展导出教程 + 用户点击后自动查找刚导出的文件”设为推荐路径，Chrome/Edge 数据库直接读取下沉为高级实验方式。
  - 用户当前仍在下载长视频，已明确允许先修改代码；正式 EXE 打包必须等用户主动确认下载完成后再执行，避免覆盖正在运行且被占用的产物。
  - 新增软件内 5 步教程：官方扩展商店、yt-dlp 官方说明、无痕唯一标签页、`robots.txt` 地址复制、Netscape + `Export` 提示，以及 Cookie 安全和账号限流风险说明。
  - 新增桌面/下载目录自动发现：仅检查顶层最近 48 小时 `.txt`，验证文件大小、Netscape 头、YouTube/Google 域和账号 Cookie 名称；选择最新有效文件，保留手动选择备用。
  - 新增空文件、非 Cookie、游客 Cookie、过期文件和新旧候选选择测试；不在日志或返回状态中输出 Cookie 值。
  - 隐藏 GUI 回归确认登录设置有 8 个预期按钮，教程有 5 个操作按钮；教程请求尺寸为 812×689，小于当前屏幕 1707×1067。
  - 首次 GUI 回归因只搜索根窗口直接子控件而未找到嵌套教程窗口，已改为递归遍历控件树后通过；没有修改产品代码规避测试。
  - 全项目 53 项测试、`pip check`、语法检查和 Git 空白检查全部通过。
  - 用户确认下载完成并授权关闭关不掉的旧窗口；只对精确匹配 `dist\融合下载器.exe` 的 PID 65160 请求正常关闭，进程已退出，未终止其他软件。
  - 通过正式 `build_exe.ps1` 生成 250,151,527 字节单文件 EXE，SHA-256 为 `908CCC40A49D1586B0AB99095140B4BEFEE1489BF2AA0C2F3CFE212F3CDDF2A4`。
  - PyInstaller 归档确认包含 YouTube 下载器、Cookie 加载、YouTube extractor、EJS、Deno、curl-cffi、FFmpeg 和 FFprobe；隐藏启动 12 秒正常，测试进程随后关闭。
  - 使用 Windows 界面控制对打包后的真实 EXE 做最终回归：切换到 YouTube，打开登录设置和 5 步 Cookie 教程，确认推荐入口、官方链接按钮、`robots.txt` 复制和自动导入入口均可见且布局完整；未点击外部链接、未读取或修改真实 Cookie、未启动下载，最后正常关闭测试窗口。
  - 最终审计发现 `downloaders/bilibili.py` 与 `tests/test_bilibili.py` 的修改时间为 15:44:50，晚于本轮 EXE 的 15:41:39；这两处 Bilibili 并行下载改动不是本阶段产生，已保留且未回退，也没有重新混入已验证的 YouTube 教程安装包。
  - 针对出现并发改动后的当前工作区重新运行全量检查：57 项测试、语法与 Git 空白检查仍全部通过；正式 EXE 继续对应 15:41:39 时已经完成验收的 YouTube 教程源码状态。

## 会话：2026-07-24（YouTube 可选登录态）

### 阶段 14：YouTube 可选登录态与反机器人错误分级
- **状态：** complete
- 执行的操作：
  - 完整读取 `planning-with-files-zh` 并手动运行会话恢复脚本；工作区初始干净。
  - 用当前项目 `yt-dlp 2026.07.04` 对用户链接和此前成功链接做仅解析复现，两者均返回相同 HTTP 429、Visitor Data/PO Token 缺失和机器人确认错误。
  - 核对 yt-dlp 当前 Cookie、YouTube OAuth 和 PO Token 文档；确认不能恢复被 Google 拒绝的自动化登录窗口。
  - 选定匿名默认、Firefox 自动读取、cookies.txt 导入备用和独立错误分级的实施方案。
  - 实现 `YouTube登录态/` 配置、Firefox 正常浏览器入口、Chrome/Edge 现有状态尽力读取、Netscape Cookie 文件过滤导入与清除。
  - 当前开发机未安装 Firefox；为覆盖本机 Chrome/Edge 用户补充实验性自动读取，但不读取真实用户 Cookie做无人值守测试。
  - 将 Cookie 通过 yt-dlp 的 `cookiesfrombrowser`/`cookiefile` 接入，不放入全局 Header；登录模式批量解析改为逐个执行。
  - 完成 22 项 YouTube 聚焦测试、语法与 Git 空白检查；Cookie 文件加载测试只使用合成值，返回状态不泄露 Cookie 内容。
  - GUI 隐藏回归通过：YouTube 只显示自己的登录设置和检查按钮，设置窗口六个入口完整，匿名状态文案正确。
  - 使用用户真实链接做只解析回归，没有下载媒体；当前仍为 HTTP 429 + PO Token Visitor Data 缺失 + 机器人确认，友好错误已正确归类并给出登录/网络边界。
  - 全项目 49 项测试、`pip check`、语法检查和 Git 空白检查通过。
  - 重新打包成功，最终 EXE 250,143,328 字节，SHA-256 `F7B8DBAE76035C4AE2563FA4FB7AD524999B7E890626A2603DC5DBF11F4A4261`。
  - PyInstaller 归档确认包含 YouTube 下载器、yt-dlp Cookie 与 YouTube extractor、EJS、Deno、FFmpeg/FFprobe；最终 EXE 隐藏启动 12 秒正常，测试进程随后精确关闭。
  - 登录态真实有效性需要用户在 GUI 中主动操作；本轮没有擅自读取真实 Chrome/Edge Cookie，也没有创建真实登录态文件。
  - 规划完成检查器仍因中文阶段格式报告 `0/0`；人工检查 `task_plan.md` 未勾选项为 0，阶段 1–14 均为 complete。
- 创建/修改的文件：
  - `.gitignore`
  - `AGENTS.md`
  - `README.md`
  - `app.py`
  - `downloaders/youtube.py`
  - `services/task_runner.py`
  - `tests/test_youtube.py`
  - `task_plan.md`
  - `findings.md`
  - `progress.md`

## 会话：2026-07-23（TikTok 提交）

### 阶段 13：TikTok 功能提交与推送
- **状态：** complete
- 执行的操作：
  - 用户明确授权提交并推送 TikTok 功能。
  - 复核当前分支为 `main`，远端 `origin/main` 仍为 `eabfa6b`，与本地基线一致，无并发冲突。
  - 提交前重新运行 43 项自动化测试、`pip check` 和 Git 空白检查，均通过。
  - 待提交范围为 11 个 TikTok 相关源码、测试、文档和项目状态文件；`build/`、`dist/`、测试媒体和登录态不进入提交。
  - 暂存区空白与敏感信息扫描通过；创建 TikTok 功能提交并推送 `origin/main`，随后核对本地 HEAD 与远端分支引用一致。

## 会话：2026-07-23（TikTok）

### 阶段 12：TikTok 单作品最高质量视频下载
- **状态：** complete
- 执行的操作：
  - 恢复三个项目状态文件并核对 Git；工作区干净，当前 `main` 为 `eabfa6b`。
  - 读取当前 GUI、统一调度、YouTube 下载器、打包脚本、依赖和 README，确认 TikTok 应作为独立平台模块接入。
  - 读取当前 yt-dlp 嵌入与格式选择文档，确认单作品使用 `YoutubeDL`、`bestvideo*+bestaudio/best` 和输出模板。
  - 使用用户提供的真实链接进行不下载解析：公开访问成功，11 个候选，最高为 1080×1920 H.265 + AAC MP4，带水印候选优先级更低。
  - 第一次真实下载已生成 1,580,173 字节最高质量文件，但输出路径中的 🙂 无法由 PowerShell GBK 控制台编码，日志回调在成功移动文件后抛错，任务被误报失败。
  - 同次下载出现 yt-dlp 浏览器模拟目标缺失提示；决定加入 `curl-cffi` 正式依赖与 PyInstaller 收集，而不是把当前链接偶然成功当作所有电脑都稳定。
  - 修复日志编码边界并增加回归测试；安装 `curl-cffi 0.15.0` 后，真实下载不再出现浏览器模拟目标缺失提示。
  - 修复后源码真实下载成功：格式 `bytevc1_1080p_542129-1`，1080×1920 HEVC + AAC，23.317 秒，1,580,173 字节，`watermarked=false`；独立 FFprobe 通过。
  - GUI 隐藏回归确认 TikTok 只保留“视频媒体”和“单个”，批量按钮禁用、登录控件隐藏，多个链接输入也只构建第一个作品任务。
  - 全项目 43 项自动化测试、`pip check`、语法检查和 Git 空白检查通过。
  - 重新打包 250,112,193 字节单文件 EXE；清单确认内置 TikTok extractor、curl-cffi、Deno、FFmpeg 和 FFprobe。
  - 使用系统临时目录中的最终 EXE 副本完成 GUI 端到端真实下载，界面报告成功 1、失败 0；独立 FFprobe 再次确认 1080×1920 HEVC + AAC，临时副本与媒体随后清理。
- 创建/修改的文件：
  - `downloaders/tiktok.py`（创建）
  - `tests/test_tiktok.py`（创建）
  - `services/task_runner.py`
  - `app.py`
  - `requirements.txt`
  - `build_exe.ps1`
  - `README.md`
  - `AGENTS.md`
  - `task_plan.md`
  - `findings.md`
  - `progress.md`

## 会话：2026-07-23

### 阶段 11：项目经历归档、提交与推送
- **状态：** complete
- 执行的操作：
  - 完整读取 `project-retrospective-writer`，并复核 Obsidian 的 `SOUL.md`、`CLAUDE.md`、经历模板和经历索引。
  - 按项目名、仓库名和核心关键词检索 `学习/经历/`，确认唯一主经历为 `融合下载器从抖音收藏夹工具到多平台高质量下载工作台 2026-04-12.md`。
  - 更新主经历顶部定位、平台标签、演化主线和阶段结果；追加 YouTube 公开最高质量、Deno/EJS/FFmpeg 独立打包、移除脆弱登录、并行 Range 速度优化和真实验收结果。
  - 复核 `学习/经历/index.md` 已有正确入口，因此不重复修改索引；未创建或修改 `生活/复盘/`。
  - 最终运行 31 项自动化测试，全部通过；`pip check`、语法检查和 Git 空白检查均通过。
  - 审计 11 个待提交文件，未发现真实 Cookie、token、Authorization、密码或测试链接中的私有分享参数；构建产物、下载结果、虚拟环境和平台登录态均未进入提交范围。
  - 将 YouTube 功能与项目文档提交到 `main` 并推送 `origin/main`；推送后对比本地 HEAD 与远端分支引用，确认完全一致。
- 创建/修改的文件：
  - `D:/Homework/Obsidian/学习/经历/融合下载器从抖音收藏夹工具到多平台高质量下载工作台 2026-04-12.md`
  - `task_plan.md`
  - `progress.md`

## 会话：2026-07-22

### 阶段 10：YouTube 下载速度诊断与优化
- **状态：** complete
- 执行的操作：
  - 完整读取文件规划技能，恢复三个状态文件并运行 session catchup；确认工作区仍是前序 YouTube 功能的未提交改动，没有覆盖用户文件。
  - 按关键词检索跨项目经验，仅将“YouTube 4 MiB 分段可稳定完成 4K50”和“CDN Range 需现场验证”作为待验证假设。
  - 初步代码审计确认：YouTube 固定 4 MiB HTTP 顺序分段；`max_workers` 仅进入 `concurrent_fragment_downloads`，可能无法加速测试链接的普通分离媒体流。
  - 第一次 32 MiB 单 Range 基准在当前 CDN 节点 45 秒无响应并超时；已停止该大块策略，下一轮改为重新解析后的小 Range 顺序/并行对照，并让每种策略独立报告失败。
  - 第二轮基准成功：解析 4.551 秒；8 MiB 视频数据用 1 MiB 顺序 Range 仅 0.34 MiB/s，4 路并行为 1.16 MiB/s，8 路并行为 2.38 MiB/s，均为正确 206 Range，当前瓶颈已定位为单连接/顺序传输。
  - 第三轮块大小基准成功：8 路并行下 2 MiB 块为 4.64 MiB/s，4 MiB 块为 7.08 MiB/s；决定保留 4 MiB 稳定块大小并实现 2/4/8 路有界并行，而不是放大单连接。
  - 实现 YouTube 普通 HTTP 流有界并行 Range 下载：逐块校验 206、`Content-Range` 和长度，精确预分配文件，限制 8 路与 3 次重试，失败时清理不完整流并重新解析后回退 yt-dlp 稳定分段。
  - 新增分块覆盖、Cookie 剥离、Range 写入、重试、分轨合并和稳定回退测试；31 项全项目测试、`pip check`、语法检查和 Git 空白检查通过。
  - 使用测试链接完成 8 路真实 4K50 端到端下载：总耗时 44.132 秒；视频 231.5 MiB、平均 7.55 MiB/s，音频 1.9 MiB、0.51 MiB/s，合并 0.3 秒；最终 244,696,376 字节 MKV，FFprobe 确认 3840×2160/50fps VP9 + AAC。
  - GUI 速度档位回归通过：stable/balanced/fast 分别返回 2/4/8 路，YouTube 仍仅显示“视频媒体”和公开内容边界说明。
  - 默认均衡档补充测速通过：4 路下载 16 MiB 用时 4.115 秒，平均 3.89 MiB/s；用户无需手动切快速档也能获得明显提速。
  - 首次最终打包在覆盖现有 `dist/融合下载器.exe` 时遇到 `WinError 5`；构建分析和归档阶段已完成，当前先检查该精确 EXE 的占用进程，不原样重复构建。
  - 确认旧版 EXE 由用户窗口占用后未强制终止；先生成并验证并排优化版。用户关闭旧窗口后，以相同 SHA-256 的已验证优化版安全替换标准 `dist/融合下载器.exe`，并清理本轮创建的临时并排副本。
  - 最终标准 EXE 为 244,045,920 字节，SHA-256 `665A2B6BF07A5BA118F852011C1FF061D8D114DFB899F9476B37A5DCD3BE5FB8`；归档含 YouTube 模块、requests、Deno、EJS、FFmpeg/FFprobe，隐藏启动 12 秒正常。
  - 按 `experience-summary-writer` 合并更新旧条目“能列出格式不代表最高流可稳定且快速下载”，补充单连接限速、有界并行 Range、逐块验证、稳定回退和真实速度证据，没有新增重复条目。

### 阶段 9：移除 YouTube 登录
- **状态：** complete
- 执行的操作：
  - 按文件规划技能恢复三个状态文件，运行 session catchup，并检查当前 Git 未提交改动。
  - 查看用户截图，确认 Google 拒绝自动化/调试浏览器登录；公开视频下载能力本身未受影响。
  - 明确本阶段边界：YouTube 仅支持公开可访问内容，保留公开最高质量、Deno/EJS、4 MiB 分段、FFmpeg 合并和 FFprobe 验证。
  - 删除 YouTube 浏览器 profile、登录窗口、Cookie 捕获/注入与登录态读取代码；下载函数和统一调度不再接收账号 Cookie。
  - GUI 移除“登录 YouTube”按钮；切换到 YouTube 时隐藏“检查登录状态”，并显示“仅支持公开内容”的明确说明。
  - 更新 README、`.gitignore` 和 YouTube 测试，新增无登录 API、公开模式日志及受限内容错误边界检查。
  - `pip check` 通过；25 项自动化测试、全项目语法检查和 Git 空白检查通过，登录代码关键词扫描仅命中“API 不存在”的反向测试。
  - 四平台 GUI 回归通过：YouTube 隐藏全部登录/检查按钮，其他三个平台仍只显示各自登录入口；YouTube 状态文案明确公开内容边界。
  - 对测试链接完成公开模式实时解析：仍选择 `315+140`，即 3840×2160/50fps VP9 + AAC，Deno/EJS 警告为 0；本次不重复下载 244 MB 媒体。
  - 重新打包成功；首次归档关键词检查因假定 `downloaders.youtube` 会以明文条目出现而提前失败，已记录并改为检查归档实际命名与 PyInstaller TOC，不重复原假设。
  - 改用 `pyi-archive_viewer -r -b` 递归检查后确认归档包含 `downloaders.youtube`、YouTube extractor、`yt_dlp_ejs`、Deno、FFmpeg 和 FFprobe；新 EXE 为 244,038,713 字节，隐藏启动 12 秒正常。
  - 按 `experience-summary-writer` 回顾相关旧条目：“YouTube 下载｜能列出格式不代表最高流可稳定下载”和“yt-dlp 登录态｜Cookie 不要放进全局请求头”仍在各自适用边界内有效，无需合并、更新或删除；本轮产品取舍属于项目决策，没有另写跨项目经验。
  - 最终运行规划完成检查器仍因中文阶段格式报告 `0/0`；人工核对阶段 1-9 均为 complete，无 pending/in_progress，并以测试、实时解析、GUI 和打包证据完成审计。

### 阶段 8：YouTube 最高质量视频下载
- **状态：** complete（登录部分已由阶段 9 移除）
- 执行的操作：
  - 完整读取文件规划技能并恢复 `task_plan.md`、`findings.md`、`progress.md`。
  - 检查项目规则、Git 状态与现有 Bilibili、调度、GUI、README 和测试集成点；工作区初始为干净状态。
  - 将 YouTube 新目标作为阶段 8 合并到现有规划体系，没有创建第二套 memory bank。
  - 对测试链接完成未登录元数据探测：最高格式为 4K/50fps VP9 + AAC，但收到“缺少受支持 JavaScript 运行时、部分格式可能缺失”的弃用警告。
  - 核对 yt-dlp 官方 EJS 指南与本机环境：当前只有 Node 24.13.0，目标机不能依赖系统 Node；需要把运行时与 `yt-dlp-ejs` 纳入打包方案。
  - 安装并验证 `yt-dlp[default,deno]`：Deno 2.9.3 与 `yt-dlp-ejs 0.8.0` 正常工作；测试链接解析为 4K50 VP9 + AAC，且不再产生 JavaScript 运行时警告。
  - 用最低质量完成一次系统临时目录下载实验，确认合并后路径可由 `requested_downloads[].filepath` 获取，临时成品随目录自动清理。
  - 完成实现方案：独立 `downloaders/youtube.py`、域名受限 CookieJar、Deno/EJS 打包、统一 MKV 无重编码合并、FFprobe 验证、调度与 GUI 媒体平台分支。
  - 首次真实最高质量下载在视频流阶段返回 `HTTP 403`；元数据、Deno/EJS 和格式选择均成功，已停止原样重试并进入媒体访问上下文诊断。
  - 检索经验总结未发现可直接套用的 YouTube 403 条目；仅将“先核对签名 URL 请求上下文”作为假设。
  - 重新解析后验证 4K 视频与最佳音频均可普通 GET/Range 访问；采用已现场验证的 4 MiB HTTP 分段后再做完整下载，不原样重复失败路径。
  - 第二次真实下载成功：格式 `315+140`，3840×2160/50fps、VP9 + AAC、244,696,376 字节 MKV；FFprobe 内部验证通过，系统临时目录结束后媒体自动清理。
  - GUI 回归通过：YouTube 仅显示“视频媒体”，仅 YouTube 登录与检查按钮可见，IDM/评论/收藏参数禁用，链接输入保持可用。
  - 四平台切换回归通过：抖音、小红书、Bilibili 与 YouTube 各自只显示对应登录入口，原平台功能和输入状态未被 YouTube 分支覆盖。
  - 依赖检查、25 项自动化测试、全项目语法检查和 Git 空白检查全部通过。
  - 重新生成 244,045,149 字节单文件 EXE；归档确认包含 `downloaders.youtube`、完整 YouTube extractor、`yt_dlp_ejs` 脚本、Deno、FFmpeg 和 FFprobe，隐藏启动 12 秒保持正常。
  - 规划完成检查器仍因中文阶段格式报告 `0/0`；人工复核阶段 1-8 均为 complete，无 pending/in_progress，以真实下载、测试与打包证据完成审计。
- 创建/修改的文件：
  - `downloaders/youtube.py`（创建）
  - `tests/test_youtube.py`（创建）
  - `services/task_runner.py`
  - `app.py`
  - `requirements.txt`
  - `build_exe.ps1`
  - `.gitignore`
  - `README.md`
  - `AGENTS.md`
  - `task_plan.md`
  - `findings.md`
  - `progress.md`

## 会话：2026-07-17

### 阶段 7：Cookie 传递与日志降噪修复
- **状态：** complete
- 执行的操作：
  - 复核用户日志，确认 42 行红字由同一弃用提示以“提示/错误”各重复 21 次造成，下载流程最终完成。
  - 从 `yt-dlp` 全局 `http_headers["Cookie"]` 改为域名受限的内存 CookieJar。
  - 日志器对相同提示去重，并将进入 `error()` 的弃用提示降为普通提示；真实错误仍保留“Bilibili 错误”。
  - 增加 Cookie 域作用范围、提示去重和真实错误保留测试。
  - 运行 15 项自动化测试与全项目语法检查，全部通过；Git 空白检查通过。
  - 对用户最初提供的 BV 链接完成真实不下载解析：当前源码环境未读取到登录态，仍选择 `30080` 1080p/25fps AVC + `30280` AAC；自定义 logger 输出 0 条，弃用提示与错误均为 0 条。
  - 重新生成 200,289,103 字节单文件 EXE；归档确认包含 Bilibili 模块、yt-dlp Bilibili 解析器、FFmpeg 与 FFprobe，隐藏启动 12 秒保持正常。
  - 按 `experience-summary-writer` 复核相关旧条目：CDN 节点轮换与 B站 IDM 请求头条目仍有效，无需合并或删除；新增一条跨项目 CookieJar/日志分级经验，未记录任何真实 Cookie。
  - 首次暂存后的空白检查发现 `tests/__init__.py` 末尾多余空行；已删除并安排重新检查，源码逻辑不受影响。
  - 重新暂存后 Git 空白检查通过；提交文件清单、忽略目录和敏感信息扫描通过，未包含构建产物、登录态、下载媒体或真实 Cookie。
  - 规划技能完成检查器仍因中文阶段格式报告 `0/0`；人工复核 7 个阶段均为 complete，无 pending/in_progress，以实际测试和打包证据完成审计。
- 创建/修改的文件：
  - `downloaders/bilibili.py`
  - `tests/test_bilibili.py`
  - `task_plan.md`
  - `findings.md`
  - `progress.md`

### 阶段 1：恢复上下文与项目盘点
- **状态：** complete
- **开始时间：** 2026-07-17
- 执行的操作：
  - 完整读取 `planning-with-files-zh` 技能及三个模板。
  - 检查根目录项目规则、README、依赖、目录结构和 Git 状态。
  - 检索 Codex 项目记忆中与当前下载器有关的旧审阅索引；将其仅作为待复核假设。
  - 创建本次任务的三个项目级规划文件。
  - 初步定位 GUI、统一调度、平台下载器以及 `ffmpeg/ffprobe` 打包逻辑。
  - 手动执行会话恢复脚本；未发现需要补同步的旧会话内容。
  - 创建项目长期规则 `AGENTS.md`，将稳定约定与本次任务状态分离。
- 创建/修改的文件：
  - `task_plan.md`（创建）
  - `findings.md`（创建）
  - `progress.md`（创建）
  - `AGENTS.md`（创建）

### 阶段 2：Bilibili 技术方案与验收设计
- **状态：** complete
- 执行的操作：
  - 明确独立平台模块、统一批量调度与 GUI 最小功能入口的集成方式。
  - 核实本机 FFmpeg/FFprobe 可用，虚拟环境尚未安装 Bilibili 解析依赖。
  - 安装并验证 `yt-dlp` 2026.07.04。
  - 对给定 BV 链接完成不下载媒体的格式探测，确认 1080p DASH 视频与最佳 AAC 音频需合并。
- 创建/修改的文件：
  - 暂无。

### 阶段 3：功能实现
- **状态：** complete
- 执行的操作：
  - 新增 Bilibili 平台模块：链接识别、最高质量选择、临时下载、FFmpeg 合并、FFprobe 音视频验证和结构化报告。
  - 接入统一批量调度，保持单条失败聚合。
  - GUI 新增 Bilibili 平台且只暴露“视频媒体”，隐藏无关的登录和收藏控制。
  - 更新依赖、README 和 PyInstaller 收集配置。
  - 新增 Bilibili URL、媒体验证、命名和任务调度测试。
- 创建/修改的文件：
  - `downloaders/bilibili.py`（创建）
  - `services/task_runner.py`（修改）
  - `app.py`（修改）
  - `requirements.txt`（修改）
  - `build_exe.ps1`（修改）
  - `README.md`（修改）
  - `tests/__init__.py`（创建）
  - `tests/test_bilibili.py`（创建）

### 阶段 4：测试与真实链接验证
- **状态：** complete
- 执行的操作：
  - 运行 6 项自动化测试，首次发现并修复中文分享文案 URL 截断问题后全部通过。
  - 运行全项目语法检查和 Git 空白错误检查，均通过。
  - 将打包前置条件收紧：构建机缺少 FFmpeg 或 FFprobe 时直接停止，避免生成残缺 EXE。
  - 诊断三条 DASH 流的 Range 响应，确认均可访问；将质量排序调整为 1080p 高码率 AVC + 最佳 AAC，并启用分段下载超时/重试。
  - 验证 512 KiB、1 MiB、4 MiB 分段均成功，最终采用 4 MiB；已清理两次中断留下的精确临时目录。
  - 确认播放信息含 2 个备用 CDN 但 `yt-dlp` 公开格式未保留；实现节点补取、64 KiB 健康探测和下载中自动轮换。
  - 使用给定 BV 链接完成端到端真实下载；音频首节点中断后自动恢复，最终 FFmpeg 合并与 FFprobe 内部验证通过。
  - 独立 FFprobe 复核成品含 1920×1080 H.264 视频流和 AAC 音频流，时长 1973.269 秒，且无 `.bilibili-*` 临时目录残留。
  - 实际构建 200,281,429 字节单文件 EXE；归档确认含 Bilibili 模块、yt-dlp Bilibili 解析器、FFmpeg/FFprobe，隐藏启动 12 秒保持正常。
  - 手动回归 GUI 的 Bilibili 状态：仅显示“视频媒体”，隐藏两个登录按钮并禁用无关高级参数。
- 创建/修改的文件：
  - `downloaders/bilibili.py`（修复中文标点边界）
  - `build_exe.ps1`（强制检查并捆绑媒体工具）

## 测试结果
| 测试 | 输入 | 预期结果 | 实际结果 | 状态 |
|------|------|---------|---------|------|
| 初始工作区检查 | `git status --short` | 无用户未提交修改 | 输出为空 | 通过 |
| Bilibili 元数据提取 | 用户给定 BV 链接 | 可识别视频并列出最高质量流 | 识别为 1080p AVC 视频 + 128.6 kbps AAC 音频 | 通过 |
| 自动化测试 | `unittest discover` | 全部通过 | 12 项通过 | 通过 |
| 语法检查 | `compileall` | 无语法错误 | 无输出，退出码 0 | 通过 |
| Git 空白检查 | `git diff --check` | 无空白错误 | 退出码 0，仅提示现有换行转换策略 | 通过 |
| 真实视频下载 | 用户给定 BV 链接 | 最高可用画质、含音频、无临时残留 | 283,189,702 字节 MP4；1920×1080/25fps/H.264/AAC；格式 30080+30280 | 通过 |
| 独立媒体复核 | 下载成品 | 视频+音频、时长与大小正常 | 2 条流；1973.269 秒；283,189,702 字节 | 通过 |
| GUI 平台状态 | 切换到 Bilibili | 仅视频媒体、隐藏无关登录/参数 | 状态符合预期 | 通过 |
| 单文件打包 | `build_exe.ps1` | 生成可独立启动 EXE并内置全部依赖 | 200,281,429 字节；归档/启动检查通过 | 通过 |
| 未登录质量重验 | 最终质量常量 + 用户 BV 链接 | 不写死 1080p，选当前权限最高档 | 30080 1080p/25fps + 30280 AAC | 通过 |
| 登录 GUI 状态 | 切换到 Bilibili | 显示 Bilibili 登录/检查按钮，隐藏其他平台按钮 | 符合预期 | 通过 |
| Cookie 域边界 | 自动化测试 | 账号 Cookie 不进入媒体 CDN 请求 | 测试通过 | 通过 |
| 最终 EXE 打包 | 最终源码 | 内置依赖并可独立启动 | 200,286,867 字节；归档与启动检查通过 | 通过 |
| CookieJar 与日志回归 | 15 项自动化测试 | Cookie 仅作用于 Bilibili、提示去重、真实错误保留 | 15 项全部通过 | 通过 |
| 修复后真实格式解析 | `BV1oHNv6kEzB` | 最高可用格式不变，弃用警告消失 | 30080+30280；弃用提示 0 条、错误 0 条 | 通过 |
| YouTube 元数据与 EJS | `EvjZ7ckgYTg` | Deno/EJS 可解析完整格式且无运行时警告 | 31 个格式；最高 315 4K50；警告 0 条 | 通过 |
| YouTube 自动化测试 | 全项目测试集 | URL、质量、Cookie 域、调度、成品路径均通过 | 25 项全部通过 | 通过 |
| YouTube 真实最高质量下载 | `EvjZ7ckgYTg` | 账号当前可用最高视频+最佳音频、无重编码合并 | 315+140；3840×2160/50fps/VP9/AAC；244,696,376 字节 MKV | 通过 |
| YouTube Cookie 域边界 | 合成登录态 | 账号 Cookie 不进入 googlevideo CDN | youtube.com/google.com 可用；googlevideo.com 无 Cookie | 通过 |
| YouTube GUI 状态 | 切换到 YouTube | 仅视频媒体、显示 YouTube 登录入口、隐藏无关参数 | 状态符合预期 | 通过 |
| YouTube 最终 EXE 打包 | 最终源码 | 内置 Deno/EJS/yt-dlp/FFmpeg/FFprobe并可启动 | 244,045,149 字节；归档确认、隐藏启动 12 秒正常 | 通过 |
| YouTube 公开模式实时解析 | `EvjZ7ckgYTg` | 不读取登录态，仍选择公开最高格式 | `315+140`；3840×2160/50fps/VP9/AAC；警告 0 条 | 通过 |
| YouTube 登录移除回归 | 四平台 GUI + 25 项测试 | YouTube 无登录入口，其他平台登录功能不变 | GUI 断言、测试、语法、依赖与空白检查全部通过 | 通过 |
| YouTube 公开模式 EXE | 最终源码 | 内置全部运行依赖并可启动 | 244,038,713 字节；递归归档确认、隐藏启动 12 秒正常 | 通过 |
| YouTube 并行 Range 速度基准 | `EvjZ7ckgYTg` 格式 315 | 找出顺序传输瓶颈并验证有界并行 | 顺序 0.34 MiB/s；均衡 4 路 3.89 MiB/s；快速 8 路完整视频平均 7.55 MiB/s | 通过 |
| YouTube 优化后完整下载 | `EvjZ7ckgYTg` | 最高质量、音视频完整、失败可回退 | 44.132 秒；315+140；3840×2160/50fps/VP9/AAC；244,696,376 字节 | 通过 |
| YouTube 速度优化 EXE | 最终源码 | 内置依赖并可独立启动 | 244,045,920 字节；归档确认、隐藏启动 12 秒正常 | 通过 |

### 阶段 5：Bilibili 登录态与会员最高画质策略
- **状态：** complete
- 执行的操作：
  - 用户根据清晰度截图明确：4K/1080p60 为大会员档，希望登录后自动选择账号权限内最高档，而不是固定 1080p。
  - 核对当前实现：质量排序已满足“分辨率→帧率→码率”，但未提供 Bilibili 登录态，需补充独立登录 profile 与 Cookie 传递。
  - 经验总结技能已读取并完成相关旧条目检索；写回延后到新增登录能力完成后统一评估。
  - 新增 Bilibili 独立登录窗口、登录态读取、普通账号/大会员状态判断与任务日志。
  - Cookie 仅传给 `bilibili.com` 页面/API；媒体 CDN 请求显式删除 Cookie，并增加安全回归测试。
  - 重新验证未登录候选为 1080p/25fps + 最佳 AAC；质量策略不限制分辨率，按 `res → fps → br` 选择。
  - GUI 回归确认 Bilibili 登录与状态检查按钮显示，其他平台登录按钮隐藏，无关高级参数禁用。
- 创建/修改的文件：
  - `task_plan.md`
  - `findings.md`
  - `progress.md`
  - `downloaders/bilibili.py`
  - `services/task_runner.py`
  - `app.py`
  - `.gitignore`
  - `README.md`
  - `AGENTS.md`
  - `tests/test_bilibili.py`

### 阶段 6：文档、经验回顾与交付
- **状态：** complete
- 执行的操作：
  - 重打最终单文件 EXE，归档确认内置 yt-dlp/Bilibili 解析器/FFmpeg/FFprobe，隐藏启动 12 秒保持正常。
  - 按 `experience-summary-writer` 规则更新跨项目经验：补强 FFmpeg 打包旧条目，新增 CDN 轮换和 Windows 超时进程树条目；相关 B站 IDM 旧条目仍有效，无需删除。
  - 完成项目文档、规划文件、测试结果和交付物复核。
- 创建/修改的文件：
  - `dist/融合下载器.exe`（重新生成，Git 忽略）
  - `D:/Homework/Obsidian/经验总结.md`（跨项目经验更新）

## 错误日志
| 时间戳 | 错误 | 尝试次数 | 解决方案 |
|--------|------|---------|---------|
| 2026-07-17 | 规划 hooks 未自动生效 | 1 | 按技能文档手动执行流程 |
| 2026-07-17 | `rg` 直接接收 `*.spec` 在 Windows 返回路径语法错误 | 1 | 改用 glob 过滤或显式文件路径 |
| 2026-07-17 | URL 提取测试把中文逗号后的正文并入查询参数 | 1 | 增加中文标点截断并保留回归用例 |
| 2026-07-17 | 真实媒体下载被 10 秒外层命令时限终止 | 1 | 将外层时限改为 10 分钟并分段读取输出；临时目录负责清理中断文件 |
| 2026-07-17 | 外层终止后第一组 Python 子进程仍运行，形成重复任务 | 1 | 根据创建时间和父子 PID 精确停止遗留进程，不影响当前任务 |
| 2026-07-17 | 第二次下载在 AV1 流上超过 3 分钟仍为 0 字节 | 1 | 主动停止，改做 AV1/AVC 小范围连接诊断 |
| 2026-07-17 | 组合式强制停止/删除命令被安全策略拒绝 | 1 | 拆分为非强制停止与后续显式路径清理 |
| 2026-07-17 | 10 MiB 分段下载被 CDN 重置，3 次重试均失败 | 1 | 改为探测 512 KiB、1 MiB、4 MiB 的稳定性，不重复 10 MiB |
| 2026-07-17 | 4 MiB 配置下视频完成，但 yt-dlp 网络层的音频仍长期为 0 字节 | 1 | 保留解析/选格式能力，改用项目自有 requests Range 下载器获取视频和音频 |
| 2026-07-17 | 规划阶段状态补丁因上下文变化而两次校验失败 | 2 | 重读三个规划文件后按文件小范围合并，源码未受影响 |
| 2026-07-17 | 规划技能完成检查器对中文模板报告 0/0 阶段 | 1 | 人工核对 6 个阶段状态和全部验收证据，无未完成阶段 |
| 2026-07-22 | YouTube 最高视频流下载返回 HTTP 403 | 1 | 验证同格式 GET/Range 后改用 4 MiB HTTP 分段，第二次完整 4K50 下载成功 |

## 五问重启检查
| 问题 | 答案 |
|------|------|
| 我在哪里？ | 全部阶段已完成 |
| 我要去哪里？ | 交付给用户；会员 4K 待未来有大会员账号时实测 |
| 目标是什么？ | 集成 Bilibili 最高质量视频媒体下载并完成真实链接验证 |
| 我学到了什么？ | 见 `findings.md` |
| 我做了什么？ | 见上方会话记录 |

### 阶段 18：全平台可选最高质量作品封面
- **状态：** complete
- 已完成：
  - 检查 `git status`，确认当前 YouTube 登录与 Bilibili 并行下载相关修改尚未提交；本轮在现有工作树上增量修改，不覆盖用户内容。
  - 重读 `task_plan.md`、`findings.md`、`progress.md`，核对 GUI、`TaskOptions`、统一调度以及五个平台模块。
  - 确认选项需要覆盖单作品、评论区图片、组合功能、抖音收藏夹、小红书收藏作品和 TikTok 单作品。
  - 确定按图片实际解码尺寸验证最高质量，并将封面失败与媒体失败分离。
  - 新增 `downloaders/covers.py`，统一完成候选下载、可解码校验、实际像素择优、已有封面复用和安全命名；五个平台模块只负责提取各自候选。
  - 在 GUI 增加始终可见的“同时获取最高质量作品封面”复选框，并通过 `TaskOptions` 贯穿单作品、评论、组合与两类收藏调度；任务完成时单独汇总封面警告。
  - 增加 15 项封面专项测试；全项目 72 项测试全部通过，`compileall` 和 `git diff --check` 通过。
  - 程序化 GUI 回归确认五个平台切换时复选框始终可见，勾选后构造的 `TaskOptions.download_cover=True`；随后启动源码 GUI，人工确认布局无重叠并实际点击切换成功。
  - 使用用户提供的五个平台链接执行封面专用真实验证：YouTube 1280×720、Bilibili 1414×900、TikTok 540×960、小红书 2112×2816、抖音 640×360；五张图片均通过双重解码和人工预览。
  - 关闭本轮源码 GUI 测试进程，并清理脚本、日志和五张测试封面所在的 6 个系统 TEMP 路径；未触碰用户下载内容。
  - 重新构建 `dist/融合下载器.exe`：250,173,771 字节，时间戳 2026-07-27 10:43:49，SHA-256 `9835710D010E30DB034D7B6C21E434EB09FC75C83F77EBFBEEC19018A0DFAD2A`，晚于相关源码。
  - 本次构建的 `PYZ-00.toc` 明确包含 `downloaders.covers`、五个平台模块；EXE 同时内置 Deno、FFmpeg 和 FFprobe。
  - 正式 EXE 启动 8 秒后进程仍响应；随后用户按 Esc 停止 Windows 界面控制，因此未继续点击正式 EXE。此前同一源码 GUI 已完成可见布局和复选框点击验收。
  - 最终重跑 72 项全项目测试、语法检查和 `git diff --check`，均通过；关闭正式 EXE 测试进程并清理 2 个系统 TEMP 构建日志。
- 下一步：
  - 交付正式 EXE；不提交、不推送，保留用户当前未提交工作树。
- 验收错误：
  - 首次从系统临时目录启动真实封面脚本时，Python 未自动包含项目根目录，导入 `downloaders` 失败；脚本未进入网络阶段。已改为显式加入项目根目录后重跑。
  - 独立图片核验的 PowerShell 循环曾直接接管道导致解析失败；改为先赋值 `$rows` 后五张图片均验证通过。
  - 查询已退出的临时 Chrome 进程时 CIM 两次超时；改用精确 PID、端口和普通进程查询确认已退出。
  - `Remove-Item` 清理命令被安全策略拒绝两次；逐项验证路径位于系统 TEMP 后用 .NET 文件 API 完成清理并复查为空。
  - `pyi-archive_viewer -l` 只展示 EXE 外层资源，首次据此搜索新模块未命中；改查当前构建的 `PYZ-00.toc` 后确认模块已打入。

### 阶段 19：Apple 风格 GUI 重构
- **状态：** complete
- 已完成：
  - 检查 `git status --short`，开始时工作区干净；重读项目规则、三个规划文件和当前 `app.py`。
  - 完整读取用户指定缓存中的 `ui-ux-pro-max` 2.11.0 主规则、交付检查表与高优先级 quick reference。
  - 运行该 skill 的设计系统与 UX 搜索，确定低变化、低动效、宽松密度、键盘焦点、加载反馈和对比度边界。
  - 打开 Apple Education Store 当前页面，提炼近白背景、近黑标题、蓝色强调、宽留白与卡片分组的视觉语言。
  - 抓取当前 Tkinter GUI 实机基线；确认 1000×700 窗口下选择区发生横向截断。
  - 在 `app.py` 内建立语义颜色、圆角卡片、胶囊按钮、平台分段控件、可见焦点、按压/悬停/禁用状态和轻量滚动条，不增加第三方依赖。
  - 主窗口改为紧凑导航、单行 Hero、创建任务卡与任务状态卡；1180px 双栏、900px 自动堆叠，收藏入口和高级设置按需显示。
  - 保留五个平台切换、功能选择、单个/批量、封面开关、登录入口、检查状态、快捷键、日志、任务队列和 `TaskOptions` 构造。
  - GUI 回归覆盖五平台、收藏区、输入禁用、高级设置、日志、YouTube 设置弹窗、运行中加载文案、键盘焦点、900/1180 响应式几何和窄窗口滚动，全部通过。
  - 根据用户连续两张截图修正平台/模式按钮焦点状态：选中项焦点线与深色背景同色，未选项以整块浅灰背景表达焦点，不再出现蓝色内框。
  - 将运行日志改为默认展开，初始入口显示“隐藏运行日志”，保留原有收起与再次展开行为。
  - 实机截图在 1180×820 和 900×680 下核验，无横向溢出；默认窗口首屏可见完整“开始下载”按钮。
  - 对比度程序化核验：主文字 16.83:1、次要文字 5.07:1、蓝色链接 4.70:1、白色 CTA 文字 16.83:1。
  - 最终 72 项全项目测试、`compileall`、`git diff --check` 全部通过。
  - 焦点修正后重新构建 `dist/融合下载器.exe`：250,188,005 字节，时间戳 2026-07-27 15:03:12，SHA-256 `DF9B88EBCFD20095171058D9573FF064ABC5655046A51A208E80D6A62076211A`，晚于最终 `app.py`。
  - `PYZ-00.toc` 确认包含封面、YouTube、Bilibili、TikTok 与任务调度模块；EXE 外层归档确认内置 Deno、FFmpeg 和 FFprobe。
  - 正式 EXE 实际启动后枚举到可见 `TkTopLevel` 主窗口“融合下载器”且进程响应；测试进程和本轮 `_MEI728882` 临时目录均已清理。
- 验收错误：
  - 初始截图先后被前景 Chrome、并行启动的两个置顶窗口和 Windows 150% 缩放干扰；最终改为串行启动，并按物理屏幕/逻辑屏幕比例换算截图边界。
  - 两次截图诊断输出因 PowerShell 内嵌 Python f-string 引号转义失败；移除非必要的复杂输出后正常截图，未修改源码状态。
  - 打包命令超过 60 秒外层等待上限；检查到同一 PyInstaller 进程持续消耗 CPU后只轮询原进程，最终构建成功，没有重复启动。
  - 正式 EXE 的父引导进程没有暴露主窗口句柄；枚举同路径子进程的全部顶层窗口后确认可见主窗口，避免把隐藏 `popdown` 窗口误当启动失败。
  - 强制停止打包测试进程后本轮 `_MEI728882` 未自动清理；`Remove-Item` 被策略拒绝，验证精确路径位于系统 TEMP 且进程已退出后使用 .NET 删除并复查不存在。

### 阶段 20：自定义输出位置与最终交付
- **状态：** complete
- 已完成：
  - 核对 `app.py` 与 `services/task_runner.py`，确认所有平台都通过 `TaskOptions.output_root` 接收统一输出根目录，无需修改下载器模块。
  - 顶部导航新增“更改位置”；用户选择父目录后立即创建 `下载结果/`，底部动态显示完整目录，“打开下载结果”跟随当前选择。
  - 新增 `下载器设置.json` 持久化父目录，并加入 `.gitignore`；缺失、损坏和相对路径设置均回退到应用目录。
  - 任务开始前验证目录可创建，日志记录实际输出位置；运行中禁用位置切换，避免后台任务使用过程中改变根目录。
  - README 更新默认目录、自定义位置、自动创建和记忆行为。
  - 新增 4 项输出位置测试；全项目 76 项测试、`compileall` 与 `git diff --check` 通过。
  - 程序化 GUI 回归确认实际创建 `下载结果/`、路径标签更新、设置保存调用、`TaskOptions.output_root`、运行中禁用及日志默认展开。
  - 实际启动源码窗口并截图，顶部两个目录入口、双栏布局和默认展开日志均正常；测试进程已关闭。
  - 重新构建 `dist/融合下载器.exe`：250,190,127 字节，时间戳 2026-07-27 15:17:19，SHA-256 `80D42AEAAB11198915724AC40C56566269F3982F8C4A96B6E99162FFDC793CC0`，晚于最新 `app.py`。
  - `PYZ-00.toc` 确认任务调度、封面、YouTube、Bilibili 与 TikTok 模块；EXE 外层归档确认内置 Deno、FFmpeg 和 FFprobe。
  - 正式 EXE 启动后枚举到可见、可响应的 `TkTopLevel` 主窗口并取得最终截图；关闭后精确复查运行进程为 0、新增 `_MEI*` 残留为 0。
- 验收错误：
  - 首次源码截图只读取虚拟环境启动进程的 `MainWindowHandle`，未找到 Tk 子进程窗口；改为按标题枚举可见窗口后截图成功，并通过 `WM_CLOSE` 关闭精确测试窗口。
  - 首次正式 EXE 截图时窗口虽可见且响应，但被前景内容覆盖；第二次仅在截图瞬间将精确窗口置顶，取得正确截图后恢复并关闭，没有重复构建。

### 阶段 21：仅下载最高质量作品封面
- **状态：** complete
- 已完成：
  - 审计五个平台封面实现、`TaskOptions`、普通链接调度和抖音/小红书收藏链路，确认现有附加封面模式不能直接表达“封面失败即任务失败”。
  - GUI 新增“仅下载最高质量作品封面”；启用后“同时保存”选项禁用，主按钮切换为“下载封面/正在下载封面…”。
  - `TaskOptions` 新增 `cover_only`；YouTube、Bilibili、TikTok 使用 `download=False` 的元数据专用入口，抖音、小红书在媒体引擎启动前返回。
  - 抖音收藏夹、小红书全部收藏和专辑显式转发 `cover_only`，覆盖单个、批量和收藏范围。
  - 封面失败在仅封面模式下进入任务失败列表；普通“媒体+封面”模式仍保持封面警告不撤销媒体成功。
  - 新增 11 项仅封面回归；全项目 87 项测试、`compileall` 与 `git diff --check` 通过。
  - Bilibili 真实仅封面验证生成 1414×900 JPEG（135,409 字节）；目录内媒体文件 0、媒体临时目录 0，系统 TEMP 测试目录已自动清理。
  - GUI 程序化回归确认选项互斥、`TaskOptions`、运行中禁用和按钮文案；`PrintWindow` 截图确认新增复选框没有破坏双栏布局。
  - 重新构建 `dist/融合下载器.exe`：250,196,370 字节，时间戳 2026-07-28 14:37:12，SHA-256 `B06521EF179E02EAC504D27238CFFAFA0BD425265EDAE8E2C5CCDD5124A0A5BE`，晚于最新源码。
  - `PYZ-00.toc` 确认统一调度、封面与五个平台模块；EXE 外层归档确认内置 Deno、FFmpeg 和 FFprobe。
  - 正式 EXE 启动后枚举到可见且响应的 `TkTopLevel` 主窗口；关闭后运行进程 0、新增 `_MEI*` 残留 0。
- 验收错误：
  - 首次 GUI 状态脚本直接比较 Tk 状态对象与字符串导致一条断言误判；输出实际状态后改为显式 `str(...)`，控件行为本身正确。
  - 两次屏幕截图被前景窗口覆盖，另一次置顶启动参数被 Windows 命令行转义拆散；最终改用 `PrintWindow` 直接读取 Tk 窗口像素，取得正确截图。
  - `PrintWindow` 保存成功后恢复 GDI 旧对象时发生句柄类型溢出；截图已完整写入，随后精确检查确认源码 GUI 进程为 0。

### 阶段 22：参考优秀案例的 Bento GUI 重构
- **状态：** complete
- 已完成：
  - 研究 CSS Design Awards、React Bits、Aceternity UI 与 Uiverse 的当前案例和组件分类，确定采用编辑式层级、Bento 分组、紧凑操作入口和低成本微交互。
  - 重构 `app.py` 视觉系统：深绿色品牌横幅、浅绿色页面背景、白色圆角主卡、Bento 选项区、浅色日志、绿色主行动和统一状态色。
  - 默认窗口改为 1280×860，双栏比例改为 5:3；页面宽度低于 1120px 自动堆叠，最大内容宽度提升为 1320px。
  - 保留五个平台、单个/批量、收藏、高级设置、输出位置、默认展开日志、同时保存封面与仅下载封面的既有入口和状态属性。
  - 源码真实窗口点击验证平台切换、两个封面对勾、仅封面互斥、主按钮文案、高级设置展开和底部操作区；未执行真实下载或登录。
  - 新设计的 GUI 专项测试、91 项全项目测试、`compileall` 与 `git diff --check` 已通过。
  - 对比度首次检查发现 `#00B176` 上的文字为 4.41:1；保留用户指定背景色，仅将文字压深为 `#063A2B`，复测目标为不低于 4.5:1。
  - 移除横幅右侧的小圆点阵列，仅保留两层大面积绿色弧形；测试固定横幅只绘制这两个椭圆，防止点阵回归。
  - 针对标题栏和任务栏小尺寸重新设计近景图标，保存源图 `app_icon_source.png`；`favicon.ico` 写入 16、20、24、32、40、48、64、128、256 九个独立尺寸，并对 48px 以下版本做轻度锐化。
  - 重新构建 `dist/融合下载器.exe`：250,424,154 字节，时间戳 2026-07-28 17:32:34，SHA-256 `680CEF296BD63F86BC641CCAE1F7D907AFC67F8A4710D08CED5E53E4FE0D2192`，晚于最终 `app.py` 和 `favicon.ico`。
  - EXE 外层归档确认内置 142,405 字节的 `favicon.ico`、Deno、FFmpeg 和 FFprobe；本次 `PYZ-00.toc` 明确包含统一调度、封面与五个平台模块。
  - 从正式 EXE 提取并人工查看 Windows 32×32 关联图标；随后启动正式 EXE，最终窗口确认标题栏采用新近景图标、横幅圆点阵列已消失，且 `F`、`STEP 01/02/05` 均未回归。
  - 关闭验收窗口后按精确 EXE 路径复查运行进程为 0。
- 下一步：
  - 向用户交付正式 EXE；不提交、不推送，保留当前未提交工作树。
- 验收错误：
  - GUI 运行时脚本首次把 `ScrolledText.winfo_manager()` 预期为 `grid`，实际代理到内部 Frame 返回 `pack`；改查外层 `grid_info()` 后确认默认日志确实已布局显示，控件行为没有错误。

### 阶段 23：抖音图文正文 Markdown
- **状态：** complete
- 已完成：
  - 核对干净工作树、现有项目规划和 `downloaders/douyin.py`、`downloaders/douyin_collection.py` 的解析与跳过链路。
  - 两个用户入口均先走公开页面/详情接口验证，再使用软件现有内置浏览器兜底读取真实详情；测试未写入用户的 `下载结果/`。
  - 确认入口一是抖音长文章，完整 Markdown 位于 JSON 编码的 `article_info.article_content.markdown`；入口二是普通图文，顶层 `desc` 被“版本过低”截断，完整正文位于 `share_info.share_desc_info`。
  - `download_note()` 自动保存 `_正文.md`，写入作者、作品 ID、稳定来源链接、可用发布时间和完整正文；相同内容复用，变化内容使用 `unique_path()` 新建而不覆盖。
  - 普通单作品在媒体跳过判断前补齐 Markdown；收藏夹只有在原图和 Markdown 都已存在时才整项跳过，纯长文章已有 Markdown 时也会跳过。
  - 修正图文/长文章背景音乐 MP3 被误判为视频的问题，音频扩展名不再进入视频候选。
  - 新增 10 项抖音专项回归；全项目 101 项测试、`compileall` 与 `git diff --check` 通过。
  - 真实入口一生成 12,601 字节长文章 Markdown，保留 9 个章节、内嵌图片与结尾；入口二生成 7,627 字节 Markdown 和 1074×673 JPEG，44 个段落及末尾话题完整且无截断提示。
  - 通过 `services.task_runner.run_task()` 走“抖音/作品媒体”真实调度，确认重复运行复用 Markdown、跳过已有原图且失败列表为空。
  - 清理本轮系统 TEMP 隔离目录中的 3 个文件（90,661 字节），复查目录不存在；未修改用户的 `下载结果/`。
  - 重新构建 `dist/融合下载器.exe`：250,429,251 字节，时间戳 2026-07-30 18:20:51，SHA-256 `416166E0DAC5AA5D87ABECE59570B3424D30C45BABAF1AC8BA204751C17BCD8E`，晚于最新抖音源码；`PYZ-00.toc` 确认包含 `downloaders.douyin`。
  - 正式 EXE 实际启动后枚举到一个可见且响应的“融合下载器”主窗口；通过正常窗口关闭退出，无强制终止，相关进程复查为 0。
- 验收错误：
  - 首次把入口一按顶层 `video` 字典判断成普通视频；进一步检查发现 `play_addr` 实为 `ies-music/*.mp3`，并在 `article_info` 中发现完整长文章结构，随即修正假设与实现。
  - 首次统一调度核验脚本误把 `items[0]` 当成带 `report` 包装的中间结果而触发 `KeyError`；读取当前 `run_douyin_urls()` 返回契约后改为直接读取作品报告，真实调度通过。
  - 打包命令的短外层等待先超时，但 PyInstaller 子进程仍持续消耗 CPU；没有重复构建，只轮询原进程直至新 EXE 产生。

### 阶段 24：抖音长文章高清图片本地化
- **状态：** complete
- 已完成：
  - 查看用户两张原始截图，确认图一是长文章远程预览图放大后模糊，图二的 Typora 规则为 Markdown 同级 `./assets`。
  - 复核当前未提交的阶段 23 源码、测试和规划文件，保留已有改动。
  - 读取真实长文章 `article_info.fe_data.image_list`，确认 7 张正文图与 7 组候选一一对应。
  - 对图一所示流程图的 6 个候选逐一下载解码；预览图为 800×500，最高候选为 1171×732，且最大有效文件为 80,586 字节。
  - 新增长文章图片本地化：逐张下载并实际解码候选，按非预览/非水印、像素面积和有效字节数选择，写入 Markdown 同级 `assets/`。
  - Markdown 图片引用统一改写为 Typora 兼容的 `./assets/...` URL 转义相对路径；单张下载失败时保留远程引用并输出独立警告。
  - 收藏夹完整性判断同时核对 Markdown、本地引用数量和资源文件，旧 Markdown 缺少本地图时会自动进入补齐流程。
  - 新增候选顺序、高清选择、本地写入、重复复用、失败回退、路径解析和收藏跳过回归；抖音专项 14 项、全项目 105 项测试全部通过，`compileall` 与 `git diff --check` 通过。
  - 真实作品 `7659165382892902650` 下载 7 张高清图，得到 7 个本地引用、0 个远程引用、0 个缺失文件、0 个图片失败；重复运行复用 Markdown 与全部资源，无 `_2` 资源副本。
  - 图一所示流程图从正文预览候选 800×500/21,112 字节提升为实际选中的 1171×732/80,586 字节。
  - 最终 `dist/融合下载器.exe` 为 250,434,809 字节，时间戳 2026-07-30 19:26:36，SHA-256 `36D66ECD850D22E4A94054E3B48022819310BFBA7CA576CDFFF873A2B4E1933A`；`PYZ-00.toc` 确认包含 `downloaders.douyin`。
  - 正式 EXE 实际启动后出现可见、响应的“融合下载器”窗口；关闭测试进程后，清理真实下载、失败构建、隔离构建和重复 EXE 共 524,098,771 字节，正式 EXE 保留。
- 验收错误：
  - 用户原正式 EXE 正在运行，隔离构建为 `融合下载器-新版.exe`，待用户关闭后再按哈希替换正式文件，未强制结束用户进程。
  - 首次把 PyInstaller `--specpath` 放到 C 盘系统 TEMP，而入口源码在 D 盘，触发跨盘 `relpath` 错误；改用项目 `build/` 下的隔离目录后成功，不重复原方案。
  - 清理命令中的 `Remove-Item` 被安全策略拒绝；逐项验证绝对路径位于系统 TEMP 或项目 `build/` 后，用 .NET 文件 API 清理并复查目标不存在。

### 阶段 25：底部输出控制与按博主分类
- **状态：** complete
- 已完成：
  - 核对截图和当前 Tkinter 布局，确认两个位置按钮仍在顶部导航，底部 footer 只有下载路径。
  - 新增统一作者目录模块，处理 Windows 非法字符、保留名、空作者和目录复用；平铺模式继续直接使用 `下载结果/`。
  - `TaskOptions` 新增默认启用的 `organize_by_author`，并贯穿五个平台的媒体、封面、评论图片与抖音/小红书收藏任务。
  - 把“更改位置 / 打开下载结果”移动到底部输出栏，增加“按博主分类（默认）/ 全部放在下载结果”选择。
  - 保存位置与保存方式共同写入原有 `下载器设置.json`，修改其中一项不会覆盖另一项。
  - 新增路径安全、平铺兼容、配置互不覆盖、底部控件归属、默认模式及五平台作者目录回归。
  - 根据用户反馈只重做底部输出栏：改为独立白色圆角卡片，路径与操作按钮同排，绿色“打开下载结果”为主行动；分隔线下方使用紧凑的“按博主分类 / 全部平铺”分段选择。
  - 源码真实窗口确认底部卡片、路径、按钮、说明和选中态无挤压；上方任务区与日志区未修改。
  - 全项目 111 项测试、`compileall` 与 `git diff --check` 通过。
  - 中英文两个正式 EXE 均更新为 250,440,440 字节，时间戳 2026-07-30 20:01:19，SHA-256 `2EB22305EF91EC74E2EBCCD0F80046E0C41A928383A9B9C234EFABD647E84AB5`。
  - 实际启动 `dist/Fusion Downloader.exe` 后出现可见、响应的“融合下载器”窗口；本轮空闲测试实例已关闭，相关进程为 0。

### 阶段 26：微信公众号与知乎文章 Markdown
- **状态：** complete
- 已完成：
  - 工作树起点为干净 `main`，HEAD `aff3148` 与 `origin/main` 一致。
  - 复核统一调度、作者目录、底部保存方式、抖音长文章高清图片和现有 111 项测试基线。
  - 微信测试页普通请求成功，确认正文、标题、公众号名和懒加载图片字段。
  - 知乎普通页面、API 和全新内置浏览器均复现 403；没有把受限响应误当正文。
  - `opencli doctor` 通过，复用当前 Chrome 登录会话精确定位到用户指定回答 ID、标题、作者和正文容器。
  - 新增 `downloaders/article_common.py`，统一正文清洗、Markdown 转换、图片多候选实际解码、本地 `assets/`、远程失败回退、作者目录和重复复用。
  - 新增 `downloaders/wechat.py`，精确解析 `#js_content`、标题、公众号名和发布时间；过滤小图标，并为 qpic `/640` 构造 `/0` 原图候选。
  - 新增 `downloaders/zhihu.py`，支持回答与专栏；依次尝试公开页面、当前 Chrome/OpenCLI 会话和 `知乎浏览器登录态/`，回答严格按 ID 定位。
  - GUI 增加“微信公众号/知乎”两个平台和“登录知乎”入口；文章模式禁用无关的媒体引擎、评论数量和封面选项，保存方式继续使用默认按作者分类或全部平铺。
  - 统一调度支持两个文章平台的单个/批量链接、失败聚合和图片并发；README、依赖与登录态忽略规则已同步。
  - 新增 10 项文章/GUI 回归；全项目 121 项测试、`compileall` 与 `git diff --check` 通过。
  - 微信真实链接重复调度两次：13,542 字正文、3 张本地配图、0 图片失败、0 缺失，第二次全部复用。
  - 知乎真实链接重复调度两次：通过当前 Chrome 会话取得 6,359 字目标回答正文、0 失败，第二次复用；未混入其他回答。
  - 实机 GUI 核验七个平台无重叠；将被截断的“文章正文（Markdown）”缩短为“文章正文（MD）”。
  - 正式中英文 EXE 均为 250,824,214 字节，时间戳 2026-07-30 20:39:16，SHA-256 `DBFC1AF1F2F39209A1210D3A5C17F673810C81D605E8D32E438B87A070F983C3`。
  - `PYZ-00.toc` 确认包含两个文章平台、共享文章核心、BeautifulSoup 和 markdownify；正式 `融合下载器.exe` 启动后出现可见、响应窗口，正常关闭后本轮新增进程为 0。
- 验收错误：
  - 首次截图脚本只检查虚拟环境 `pythonw.exe` 启动器的窗口句柄，未发现 Tk 实际运行在系统 Python 子进程；改为枚举标题为“融合下载器”的真实窗口后成功截图并正常关闭。

### 阶段 27：OpenCLI 下载窗口自动清理
- **状态：** complete
- 已完成：
  - 复现知乎下载完成后目标标签释放、OpenCLI 窗口恢复 `about:blank` 但继续显示。
  - 核对 OpenCLI 1.8.3 帮助和本机安装源码，确认 `browser close` 只负责释放标签页租约。
  - 审计所有浏览器启动点：知乎正文和抖音评论共用 OpenCLI 残留风险；其他内置 CDP 下载路径已按进程所有权清理，人工登录/教程窗口按设计保留。
  - 新增 `downloaders/opencli_browser.py`，按浏览器进程名、顶层窗口类、前后句柄和标题变化识别本次任务窗口。
  - 清理时先执行 OpenCLI 原生会话释放；仅当同一窗口已恢复为 `about:blank` 时发送正常 `WM_CLOSE`，多候选或状态变化时失败关闭。
  - 将清理器接入知乎正文、抖音评论页面和抖音评论 API 三条 OpenCLI 会话路径。
  - 新增知乎、抖音和窗口所有权回归测试；文章专项 15 项、全项目 127 项测试通过，`compileall` 与 `git diff --check` 通过。
  - 受控窗口验收识别并关闭句柄 `54137734` 的 OpenCLI 空白窗口；用户原有知乎窗口和划词翻译窗口句柄、标题均不变。
  - 用户真实知乎链接再次通过当前 Chrome/OpenCLI 会话提取到目标回答；执行前后用户窗口集合一致，0 个 `about:blank` 残留。
  - 通过统一 `run_task()` 在系统 TEMP 隔离目录完成真实 GUI 调度等价路径：1 篇成功、0 失败、Markdown 实际存在；任务后用户窗口句柄与标题不变，0 个空白窗口残留，临时目录自动清理。
  - 重新构建正式 `dist/融合下载器.exe`：250,829,318 字节，时间戳 2026-07-30 21:57:15，SHA-256 `5F5F260FDB8E76520D70FAA669C807F146DC2813883FABAADA4E27D03614A53E`。
  - `PYZ-00.toc` 确认包含 `downloaders.opencli_browser`、`downloaders.zhihu` 和 `downloaders.douyin`；正式 EXE 启动后窗口可见且响应，正常关闭后精确路径运行进程为 0。
- 验收错误：
  - 首个新增知乎测试夹具正文过短，触发原有完整性保护；加长测试正文后通过，没有削弱生产校验。
  - Python 3.14 的 `ctypes.wintypes` 未提供 `WNDENUMPROC`；改为显式声明 Win32 回调签名后，真实窗口枚举成功。

### 阶段 28：微信贴图文字与最高质量原图
- **状态：** complete
- 已完成：
  - 实测视频号短链公开页与 `get_feed_info`，确认只暴露元数据和封面；按用户“难则只实现贴图”的条件，不引入代理、根证书与电脑微信流解密。
  - 微信模块新增 `pages/image_detail` 识别；从 `#js_image_desc` 提取完整文字，从动态画廊提取作品图片，并排除头像、二维码、表情和引导图。
  - 无状态请求触发微信验证码时，自动回退当前 Chrome/OpenCLI；提取前等待正文、作者和画廊数量连续稳定，任务后释放会话和本次空白窗口。
  - qpic 原图候选剔除 `watermark`、`tp`、`usePicPrefetch`、`wxfrom` 等临时/展示参数，并继续按非水印、实际像素、有效字节数择优。
  - canonical URL 剔除动态 `poc_token`，解决相同贴图重复运行生成 `_2` Markdown 的问题。
  - 新增贴图完整查询链接、模板隔离、作者字段、验证码回退、浏览器释放、非水印原图和稳定 URL 回归测试。
  - 全项目 132 项测试、`compileall` 与 `git diff --check` 通过。
  - 真实统一调度得到作者“项琴”、1081 字 Markdown、3 张本地图（2560×3840、2560×3840、2560×1706）、0 图片失败、0 远程图片引用。
  - 第二次真实统一调度复用同一 Markdown 与 3 张图片，全部状态为 `skipped`；执行前后用户浏览器窗口一致，0 个 `about:blank` 残留。
  - 重新构建正式 `dist/融合下载器.exe`：250,837,421 字节，时间戳 2026-07-31 09:18:22，SHA-256 `F8ABB8D5625D4966C5866C2F5E02CC9393B77868B728FF92B4B92AD856B24AFC`。
  - 最新 `PYZ-00.toc` 确认包含 `downloaders.article_common`、`downloaders.opencli_browser`、`downloaders.wechat` 和 `downloaders.zhihu`。
  - 正式 EXE 实际启动后出现可见、响应的“融合下载器”窗口；通过正常窗口关闭退出，无强制终止，精确路径下本轮新增进程复查为 0。
- 验收错误：
  - 首个真实验收脚本的远程图片统计正则写错，下载已成功但摘要失败；临时目录自动清理后改用逐行图片引用统计重跑。
  - 初版浏览器脚本在首张图出现时立即返回，真实页面有时只取得 1 张图且作者未渲染；改为正文、作者和去重画廊数量连续稳定 3 秒后再提取。
  - 浏览器 URL 的临时 `poc_token` 使两次 Markdown 内容不同；规范化时剔除后重复复用通过。

### 阶段 29：微信视频号原始质量下载
- **状态：** complete
- 已完成：
  - 复核当前 `ae48a02` 提交、统一调度、媒体验证和微信文章/贴图实现。
  - 审计 `res-downloader` 的腾讯视频资源捕获、质量参数与首段 XOR 解密，以及 `wx_channels_download` 的代理注入、ISAAC64 解密、清晰度菜单与公开分享解析页面。
  - 发现并实测参考项目的公开 `/api/fetch_video_profile`：测试短链返回作者、标题、封面及短期 H.264/H.265 视频候选。
  - 对在线 H.264/H.265 完整下载并用 FFprobe 比较：二者均为 1024×576/30 FPS，H.264 为 22,105,021 字节，H.265 为 7,822,722 字节；临时文件已自动清理。
  - 按参考项目“原始视频”规则只保留 H.264 地址的 `encfilekey` 与 `token`，得到 211,789,061 字节、1920×1080/30 FPS、H.264 + AAC 的完整 MP4；探针临时文件已自动清理。
  - 新增 `downloaders/wechat_channels.py`：短链/预览链接规范化、第三方公开解析、原始候选构造、媒体 HEAD/Range 探测、在线候选回退、有界并行下载、单连接回退、作者目录、重复复用和 FFprobe 验证。
  - 将 GUI 平台从“微信公众号”收敛为“微信”，功能显示“自动识别（视频/图文）”；统一调度按 URL 自动区分视频号与公众号文章/贴图，并保留旧“微信公众号”标签的代码兼容。
  - 新增 7 项视频号 URL、解析、质量选择、失效回退、下载复用和调度测试；文章、GUI 与视频号相关 38 项测试通过，`compileall` 与 `git diff --check` 通过。
  - 使用真实统一 `run_task()` 下载测试视频：作者“曹一刀修脚”、原始质量、1920×1080、30 FPS、H.264 + AAC、211,789,061 字节、0 失败；并行平均约 6.55 MiB/s。
  - 第二次真实统一调度复用同一文件并返回 `skipped`；真实媒体验收目录随后自动清理，未写入用户的 `下载结果/`。
  - 全项目 139 项测试、`compileall` 与 `git diff --check` 通过。
  - 重新构建正式 `dist/融合下载器.exe`：250,849,102 字节，时间戳 2026-07-31 10:03:34，SHA-256 `A87DE9230EC3A259835AF35F6A3A2047D4788DC93D758B65925A12FF7BEFCAD7`。
  - 最新 `PYZ-00.toc` 确认包含 `downloaders.wechat`、`downloaders.wechat_channels` 与 `services.task_runner`。
  - 正式 EXE 实际启动后出现可见、响应的“融合下载器”窗口；通过正常窗口关闭退出，无强制终止，精确路径下本轮新增进程复查为 0。
- 验收错误：
  - 直接 Git clone 参考项目时 GitHub 连接被重置；改用 GitHub 公开 API 与 raw 源码逐文件审计，没有修改全局或持久代理设置。
  - 首个 PowerShell HEAD 对比脚本把 `foreach` 直接接管道，触发空管道解析错误；改为先收集结果再输出，未重复网络下载。

### 阶段 30：微信贴图上传原文件与无水印优先
- **状态：** complete
- 已完成：
  - 对照用户截图、真实轮播 DOM 与公开页面脚本，确认截图来自独立的 2560×3840 水印 CDN 文件。
  - 找到同画面的 2560×3840 无水印 `cdn_url`，并进一步发现 5120×7680、46,308,272 字节的 `original_info` 上传原文件。
  - 新增贴图脚本对象的平衡括号提取、JS 十六进制转义解码、重复记录合并和短链接贴图识别。
  - 每张贴图按 `original_info` 上传原文件、`cdn_url` 无水印展示图、`watermark_info` 水印兜底图构造候选；水印源清理查询参数后仍保持水印标记。
  - 公开页面缺少渲染正文时，使用 `og:title`、说明、脚本公众号昵称和图片记录补全正文，不再为该公开短链接启动浏览器。
  - OpenCLI 兜底改为等待 `#js_content` 后同步返回精简正文和画廊节点，避免整页 DOM 回传超时。
  - 微信文章专项 10 项测试和受影响文件 `compileall` 通过。
  - 真实公开读取识别《坠入凡尘》、作者“灰诚 Ashin”、9 张贴图和 9 个上传原文件地址。
  - 完整临时目录下载得到 9 张 JPEG、0 图片失败、总计 246,523,781 字节；全部报告来源为 `wechat:data-original`，逐张报告尺寸与 PIL 实际解码一致。
  - 真实图片尺寸为 4672×7008、4142×6212、3794×5691、5120×7680、4672×7008、4106×4106、2000×3000、2762×4144、4420×6632；临时目录退出后已确认删除。
  - 全项目 141 项测试、`compileall` 与 `git diff --check` 通过。
  - 重新构建正式 `dist/融合下载器.exe`：250,857,530 字节，时间戳 2026-07-31 10:42:01，SHA-256 `5720A7A9A119D4293F2A615F107B57E1D6EE121359FEBEEADD32D7A0346F00AE`。
  - 最新 `PYZ-00.toc` 确认包含 `downloaders.article_common`、`downloaders.wechat` 与 `services.task_runner`。
  - 正式 EXE 实际启动后出现可见、响应的“融合下载器”窗口；通过正常窗口关闭退出，无强制终止，精确路径下本轮新增进程复查为 0。
- 验收错误：
  - 初次只对比渲染后的首张轮播图，误以为 2560×3840 已是源站最高质量；检查页面对象后发现 `original_info` 上传原文件最高达到 5120×7680。
  - 旧 OpenCLI 脚本回传 2.5 MB 整页 HTML，真实调用在 90 秒后超时；改为公开脚本直读和精简浏览器兜底，没有重复等待同一路径。

### 阶段 31：抖音/小红书既有作品最高质量复核
- **状态：** complete
- 已完成：
  - 只读核验用户已有目录：抖音 24 张 JPEG + 正文，小红书 3 张 3024×4032 PNG + 3 段 1080×1440 Live Photo；未修改这些旧文件。
  - 通过 OpenCLI 当前 Chrome 会话取得抖音完整详情，枚举 24×6 条候选并实际解码；旧 JPEG 与今天最佳候选逐张 SHA-256 一致。
  - 同一抖音对象的 raw/noop/q95/q100 路径修改均被源站签名校验拒绝；没有把不可访问的推测路径加入代码。
  - 小红书每张 6 条候选全部实测：raw 为 HEIC，PNG 为 3024×4032 转换图，展示/预览 WebP 为 1080×1440。
  - 新增 HEIC/HEIF/AVIF `ftyp` 品牌识别、`ispe` 主图尺寸解析和原始容器优先；全尺寸 HEIC 可保留 HDR 辅助图。
  - 普通非 HEIF 图片不再因首个候选达到页面声明尺寸就提前停止，继续实际解码全部候选。
  - 移除小红书单作品/收藏级“存在任意旧文件就跳过”；图片按完整字节复用，视频/Live Photo 按当前最佳候选长度复用。
  - 新增 3 项回归测试；小红书与封面相关 30 项测试、受影响文件 `compileall`、`git diff --check` 通过。
  - 真实链接在系统 TEMP 首次得到 3 张 3024×4032 HEIC + 3 段 1080×1440 Live Photo，第二次 6/6 复用且无重复文件；退出临时目录后确认清理。
  - 在 TEMP 复制用户旧版 3 PNG + 3 MP4 后运行新版：旧 6 个文件大小不变，只新增 3 张 HEIC，Live Photo 3/3 复用，未生成 `_1/_2`。
  - 通过统一 `run_task()` 小红书作品媒体路径再次下载真实链接：1 项成功、0 失败、3 张 HEIC + 3 段 Live Photo，输出 6 个文件；TEMP 随上下文退出清理。
  - 全项目 144 项测试、`compileall` 与 `git diff --check` 通过。
  - 重新构建正式 `dist/融合下载器.exe`：250,859,403 字节，时间戳 2026-07-31 11:36:04，SHA-256 `4858D6B0BE646EDA7D3A9F8AFFF89EC6B137BFB9F671D00E56AA56875F065E54`。
  - 最新 `PYZ-00.toc` 确认包含 `downloaders.xiaohongshu`、`downloaders.wechat`、`downloaders.wechat_channels` 和 `services.task_runner`。
  - 正式 EXE 的 Tk 子进程出现可见、响应的“融合下载器”窗口；对窗口发送正常关闭后子进程和 PyInstaller 父进程均退出，本轮 `_MEI647322` 目录已自动删除。
- 验收错误：
  - 旧 Pillow 无法解码 raw，最初被表象误判为无效图片；检查 `Content-Type`、文件 magic、HEIF box 和 FFprobe Tile Grid 后确认它是有效 HEIC 原始容器。
  - OpenCLI 1.8.3 不接受技能文档示例的 `--window` 参数；改用命令级 `OPENCLI_WINDOW=background` 打开受控会话，完成读取后释放 tab lease。
  - 第一次 EXE 启动验收只读取 PyInstaller 父进程，父进程没有窗口句柄；随后按父子关系定位同路径 Tk 子进程，确认窗口可见且响应，再正常关闭两层进程。
  - 第一次统一调度摘要脚本误用其他平台的 `reports` 字段，任务已成功但打印触发 `KeyError`；临时目录清理后改按小红书真实 `items` 结构重跑通过。

### 阶段 32：失效系统代理直连回退与任务终止
- **状态：** complete
- 已完成：
  - 复核当前 Git 工作树并保留微信、小红书和文档的既有未提交改动。
  - 读取 Windows 代理注册表和 Requests 实际代理映射，确认关闭代理程序后仍继承 `127.0.0.1:10808`。
  - 定位 GUI 后台线程、统一调度、抖音/小红书候选并发和各平台流式下载循环。
  - 新增并接入任务级取消状态模块，以及仅针对不可达 loopback 代理的临时直连上下文。
  - 统一调度现按任务首个真实 URL 读取 Requests 的实际代理映射；本机代理端口不可达时仅在任务上下文中设置直连绕过，退出后精确恢复原 `NO_PROXY/no_proxy` 环境。
  - GUI 主按钮运行时变为“终止下载”；点击后变为“正在终止…”，后台发出强类型取消信号，结束后恢复“开始下载”，状态显示“任务已终止”且不弹出误导性的完成对话框。
  - 取消信号已接入统一 URL 批量、抖音/小红书单作品与收藏并发、文章图片、Bilibili/YouTube Range、TikTok/YouTube yt-dlp 进度钩子和微信视频号流式下载；直接流式媒体被取消时删除本轮不完整目标。
  - 强制子进程使用失效代理 `127.0.0.1:9`：基线稳定触发 `ProxyError`；新上下文自动直连到抖音并获得 HTTP 响应，退出后代理映射恢复为原值。
  - 使用用户截图中的 30 图作品短链，在同一强制失效代理条件下完成真实统一调度：自动直连、内置浏览器读取作品、首张图片完整保存并解码为 944×1667 后触发终止，剩余任务停止；临时目录退出后确认不存在。
  - 受控真实 Tk 顶层窗口为 1280×860、可见且响应；后台任务开始后主按钮为“终止下载/normal”，点击后线程退出、按钮恢复“开始下载/normal”、状态为“任务已终止”，完成弹窗 0 次。
  - 新增 8 项代理/取消专项测试；全项目 152 项测试、`compileall` 与 `git diff --check` 通过。
  - 正式构建完成：`dist/融合下载器.exe` 为 250,866,098 字节，时间戳 2026-07-31 23:12:30，SHA-256 `6FBEEE186AF1AB1F598262DDBB03A6C42C3996258E0C4358A90EF20D41815F78`。
  - 最新 `PYZ-00.toc` 确认包含 `services.cancellation`、`services.network_policy`、`services.task_runner` 和 `downloaders.douyin`。
  - 正式 EXE 的 Tk 子进程显示可见、响应的“融合下载器”窗口；发送正常关闭后同路径父子进程均为 0，本轮 `_MEI` 目录已自动删除。
- 验收错误：
  - 第一次构建命令误设 1 秒 shell 超时，进程被停止且旧 EXE 未变化；确认无遗留 Python/PyInstaller 进程后用 10 分钟上限重跑，91.6 秒成功完成。
  - 第一次 EXE 启动检查在子进程解包前只读到无窗口的 PyInstaller 父进程，未误报通过；等待后按精确 EXE 路径找到可见 Tk 子进程并正常关闭，最终进程数为 0。
## 阶段 30：抖音收藏夹精确 ID 与手动安全更新（in_progress）

- 复现用户 EXE 日志并脱敏读取实际返回：18 个收藏夹可见，“图片”作品接口 HTTP 200 / status_code 0 / 0 项。
- 核验 Windows 手动代理和用户环境代理均指向未启动的 `127.0.0.1:10808`；现有任务级直连回退生效。
- 通过软件专属抖音登录态打开网页收藏夹，确认网页显示“图片，共 2 作品”；对比发现浏览器数字 ID 舍入，字符串 ID 保持精确。
- 修改 `parse_collection_list()` 优先使用 `collects_id_str`；真实全链路刷新后选中精确 ID `7655694467143407401`，读取到 2 个作品。
- 新增 `app_version.py`、`services/updater.py`、`fusion_update_helper.ps1` 和 `RELEASING.md`；GUI 顶部新增手动检查更新入口。
- 更新流程包含正式 Release 过滤、数字版本比较、固定 Windows EXE 选择、大小和 SHA-256 双校验、用户两次确认、独立进程替换、旧版备份、5 秒启动验证和失败回滚。
- 小范围语法检查通过；更新器、收藏夹 ID 和 GUI 共 9 项聚焦测试通过；PowerShell 更新助手语法解析通过。
- 当前 GitHub 真实检查通过：本地 2.2.0、公开 2.1、资产 `Fusion.Downloader.exe` 且存在 SHA-256，正确判定无需降级。
- 全量 160 项单元测试通过；全项目 `compileall`、Git 空白检查和两份 PowerShell 脚本语法检查通过。
- 更新助手在随机系统临时目录完成两条真实集成验收：正常新版替换后旧版哈希备份正确且新版保持运行；故障新版 5 秒内退出后旧版哈希恢复并重新启动。夹具进程和目录均已清理。
- 首次正式打包已生成 EXE，但脚本末尾依赖 `Get-FileHash` 自动加载失败，因此未计为通过；改用 .NET SHA-256 后重新完整打包，入口退出码为 0。
- 最终 `dist/融合下载器.exe` 与 `dist/Fusion.Downloader.exe` 均为 250,879,181 字节，SHA-256 都是 `0a9e28df2d17ea64af32f2837808376d83368434bcd0680d8d81562783361ac3`。
- PyInstaller 外层归档确认包含 `fusion_update_helper.ps1`；PYZ TOC 确认包含 `app_version`、`services.updater` 和 `downloaders.douyin_collection`。
- 正式 EXE 真实窗口 1296×899、响应正常；视觉核对顶部显示 `检查更新 v2.2.0`。实际点击后弹出“已是最新版本”，证明打包态 GitHub 检查链路可用。
- 两张 GUI 验收截图已删除；本轮启动的正式 EXE 进程剩余 0。保留最终 EXE、源码、测试和用户原有配置/登录态/下载内容。
