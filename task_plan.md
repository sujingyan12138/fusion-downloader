# 任务计划：融合下载器平台扩展

## 目标
在现有 Windows GUI 下载器中持续接入并完善视频平台：已完成 Bilibili、YouTube 和 TikTok 最高质量视频下载；当前让 Bilibili 速度档位真正作用于单视频媒体传输，同时保留备用 CDN、严格分段校验、稳定回退和最高质量验收。

## 当前阶段
阶段 35：首页横幅极光光环动效（完成）

## 各阶段

### 阶段 1：恢复上下文与项目盘点
- [x] 读取规划技能并确认需手动执行恢复流程
- [x] 检查根目录规则、说明、依赖和 Git 工作区状态
- [x] 盘点现有下载器、任务调度、GUI、打包与测试结构
- [x] 创建或完善长期有效的 `AGENTS.md`
- **状态：** complete

## 阶段 38：页头品牌字体（2026-08-06）
- [x] 选择 Vercel Geist Sans SemiBold 作为英文页头字体，正文继续使用 Windows 原生中文字体。
- [x] 将标题以 Pillow 高分辨率渲染后缩小显示，避免依赖用户系统字体。
- [x] 纳入字体与 OFL 许可证，更新 PyInstaller 配置、自动化测试并重新打包。
- **状态：** complete

## 阶段 37：极简页头收敛（2026-08-06）
- [x] 移除黑洞视觉、动效和独立横幅，避免浅色工作台出现灰雾和视觉噪声。
- [x] 将窗口与页面品牌统一为居中的 Fusion Downloader，保留右侧检查更新入口。
- [x] 以浅色页头和细分隔线替代装饰性背景，保持下载表单与状态面板的可读性。
- [x] 删除不再使用的图片资源及其 PyInstaller 打包配置，完成回归检查和重新打包。
- **状态：** complete

### 阶段 2：Bilibili 技术方案与验收设计
- [x] 确认最高质量媒体的获取、音视频合并、依赖和登录边界
- [x] 确定与现有 GUI、任务调度、命名和输出目录的集成点
- [x] 记录关键决策与风险
- **状态：** complete

### 阶段 3：功能实现
- [x] 实现 Bilibili URL 识别、元数据解析和最高质量媒体下载
- [x] 集成统一任务调度与 GUI
- [x] 更新依赖、说明和打包配置
- [x] 增加自动化测试或可离线验证用例
- **状态：** complete

### 阶段 4：测试与真实链接验证
- [x] 运行静态/单元/集成测试
- [x] 使用给定 BV 链接验证下载、质量选择、音视频合并及输出命名
- [x] 检查失败提示、临时文件清理和对原平台功能的回归影响
- [x] 实际打包并验证 EXE 内置依赖与启动状态
- **状态：** complete

### 阶段 5：Bilibili 登录态与会员最高画质策略
- [x] 新增独立 Bilibili 登录窗口和登录态读取
- [x] 将 Cookie 安全传入格式解析与备用节点请求，且不发送到第三方 CDN
- [x] 验证未登录仍选 1080p，登录后按分辨率、帧率、码率自动选账号可用最高档
- [x] 更新 GUI、README、忽略规则和测试
- **状态：** complete

### 阶段 6：文档、经验回顾与交付
- [x] 更新项目文档和三个规划文件
- [x] 检查经验总结中相关旧条目，仅将经验证的跨项目经验按技能规范更新
- [x] 重新打包并验证最终 EXE
- [x] 汇总创建/修改文件、验证结果和剩余限制
- **状态：** complete

### 阶段 7：Cookie 传递与日志降噪修复
- [x] 将登录 Cookie 从原始 HTTP Header 改为 `yt-dlp` CookieJar
- [x] 对重复警告去重，并避免将非致命弃用提示误标为任务错误
- [x] 增加 Cookie 作用域与日志行为回归测试
- [x] 运行自动化测试、语法检查、真实格式解析与最终打包验证
- [x] 更新项目状态文件，审阅跨项目经验并完成提交前审计
- **状态：** complete

### 阶段 8：YouTube 最高质量视频下载
- [x] 核对测试链接、YouTube 格式选择、登录态和现有平台复用边界
- [x] 新增独立 YouTube 下载器、登录窗口和安全 CookieJar
- [x] 接入统一调度与 GUI，仅暴露“视频媒体”
- [x] 更新 README、忽略规则、打包说明和自动化测试
- [x] 用测试链接完成真实解析/下载、音视频验证和 GUI 回归
- [x] 重新打包并验证单文件 EXE
- **状态：** complete

### 阶段 9：移除 YouTube 登录
- [x] 删除 YouTube 登录窗口、登录态读取和 Cookie 传递代码
- [x] 调整 GUI、调度日志和错误提示，明确仅支持公开内容
- [x] 更新 README、忽略规则和自动化测试
- [x] 运行依赖、测试、语法、GUI 与打包验证
- [x] 回顾相关跨项目经验并完成交付审计
- **状态：** complete

### 阶段 10：YouTube 下载速度诊断与优化
- [x] 基准测量解析与最高视频流传输耗时，确认媒体传输远慢于约 4.6 秒解析
- [x] 验证顺序 Range 是当前最高视频流瓶颈，并量化有界并行收益
- [x] 实现有边界的并行 Range 下载并保留稳定回退
- [x] 增加速度策略、完整性和失败回退测试
- [x] 完成真实链接、GUI、测试、语法和打包验证
- [x] 回顾相关跨项目经验并完成交付审计
- **状态：** complete

### 阶段 11：项目经历归档、提交与推送
- [x] 按项目名检索 `学习/经历/`，确认沿用唯一主经历文件
- [x] 将 YouTube 最高质量、公开访问边界、速度优化和打包验证追加到主经历
- [x] 运行最终依赖、测试、语法与 Git 空白检查
- [x] 审计提交范围和敏感信息
- [x] 提交 downloader 改动并推送 `origin/main`
- [x] 对比本地 HEAD 与远端 `refs/heads/main`
- **状态：** complete

### 阶段 12：TikTok 单作品最高质量视频下载
- [x] 核对测试链接、yt-dlp 当前格式候选和公开访问边界
- [x] 确认仅接入单个作品链接和“视频媒体”，不扩展主页、收藏、评论或字幕
- [x] 新增独立 TikTok 下载器并接入统一调度与 GUI
- [x] 更新 README、打包提示和长期项目规则
- [x] 增加链接、质量、命名、媒体验证和调度测试
- [x] 使用测试链接完成真实最高质量下载与 FFprobe 验证
- [x] 运行全项目测试、语法检查、GUI 调度回归和打包验证
- **状态：** complete

### 阶段 13：TikTok 功能提交与推送
- [x] 复核工作区、自动化测试、依赖和 Git 空白检查
- [x] 审计并暂存 TikTok 源码、测试、文档和项目状态
- [x] 创建提交并推送 `origin/main`
- [x] 对比本地 HEAD 与远端 `refs/heads/main`
- **状态：** complete

### 阶段 14：YouTube 可选登录态与反机器人错误分级
- [x] 复核现有 GUI、YouTube 下载器、配置存储、打包和测试边界
- [x] 设计匿名默认、Firefox/Chrome/Edge 自动读取和 cookies.txt 导入/清除流程
- [x] 实现登录态配置、域名与文件格式校验，并接入 YouTube 解析下载
- [x] 将 HTTP 429、机器人验证、账号权限和 Cookie 读取失败拆分为明确中文提示
- [x] 更新 GUI、README、长期项目规则和自动化测试
- [x] 使用真实链接验证匿名失败分级，并在可用登录材料边界内验证登录态接入
- [x] 运行全项目测试、语法、依赖、GUI 和打包验证
- **状态：** complete

### 阶段 15：Windows 浏览器 Cookie 锁定实机诊断
- [x] 核对用户关闭窗口后的 Chrome、Edge 与 WebView2 实际进程状态
- [x] 区分 Cookie 数据库复制锁定与后续可能出现的应用绑定解密失败
- [x] 用户选择不终止后台进程，改用官方推荐的浏览器扩展导出 Cookie
- [x] 核对正确扩展、无痕会话与 YouTube Cookie 保活导出步骤
- [x] 用户导入正确 cookies.txt 后验证登录态和目标视频解析
- **状态：** complete

### 阶段 16：YouTube 新手 Cookie 导出引导与自动导入
- [x] 在软件内提供逐步导出教程、官方扩展链接和安全提醒
- [x] 自动查找桌面/下载目录中最近导出的有效 YouTube Cookie，并保留手动选择备用
- [x] 将 Chrome/Edge 直接读取降为高级实验方式，避免普通用户误以为稳定可用
- [x] 增加自动发现、错误提示和 GUI 入口回归测试
- [x] 更新 README、长期规则和项目状态文件
- [x] 等用户确认当前下载完成后再构建并验证正式 EXE
- **状态：** complete

### 阶段 17：Bilibili 有界并行 Range 下载与速度验证
- [x] 核对当前 Bilibili 传输层，确认 4 MiB 分块仍为串行且 `max_workers` 被忽略
- [x] 实现 2/4/8 路有界并行 Range、连接复用、分块严格校验与备用 CDN 轮换
- [x] 保留不支持并行、大小未知或分块失败时的串行稳定回退
- [x] 增加分段覆盖、乱序写回、备用节点切换和失败回退自动化测试
- [x] 运行 19 项 Bilibili 专项测试、57 项全项目测试、语法与空白检查
- [x] 使用系统临时目录完成最高质量真实下载、FFmpeg 合并和 FFprobe 验证
- [x] 在最终 Bilibili 源码之后重新构建并验证正式 EXE
- **状态：** complete

### 阶段 18：全平台可选最高质量作品封面
- [x] 复核当前未提交改动、GUI 参数链路、四类功能分支和五个平台解析结果
- [x] 增加全局“同时获取最高质量作品封面”可选项并贯穿统一调度
- [x] 为 YouTube、Bilibili、TikTok、抖音和小红书实现封面候选选择、实际像素验证与安全命名
- [x] 让单作品、评论区组合功能及抖音/小红书收藏入口遵守同一选项
- [x] 增加封面质量、失败隔离、参数传递、命名和重复运行测试
- [x] 使用用户提供的 YouTube、抖音、小红书、Bilibili 和 TikTok 链接完成真实封面验证
- [x] 运行全项目测试、语法检查、GUI/调度回归和文档核对
- **状态：** complete

### 阶段 19：Apple 风格 GUI 重构
- [x] 核对当前工作区、GUI 结构、项目规则和既有规划文件
- [x] 读取 `ui-ux-pro-max-skill`，生成桌面下载工具设计系统并完成高优先级 UX 检查
- [x] 检查 Apple Education Store 当前页面结构并抓取现有 GUI 基线
- [x] 重构主窗口的视觉层级、平台选择、任务操作区与状态反馈区
- [x] 保留现有平台切换、登录、线程队列、下载选项和快捷键行为
- [x] 完成自动化测试、语法检查、小窗口布局、键盘路径和实机截图验收
- **状态：** complete

### 阶段 20：自定义输出位置与 GUI 反馈收尾
- [x] 消除平台/模式按钮点击时的蓝色内框，保留中性可见焦点
- [x] 将运行日志改为默认展开，保留手动隐藏入口
- [x] 在顶部增加“更改位置”，自动在所选位置创建 `下载结果/`
- [x] 记住所选保存位置，并让任务调度与“打开下载结果”使用当前目录
- [x] 增加设置解析测试，完成 76 项测试、语法检查与源码 GUI 回归
- [x] 重新构建并实际启动最终 EXE
- **状态：** complete

### 阶段 21：仅下载最高质量作品封面
- [x] 新增独立复选框与“下载封面”运行状态
- [x] 五个平台单个、批量及两类收藏任务均支持仅封面模式
- [x] 仅封面失败按任务失败处理，不触发视频、图片媒体或评论下载
- [x] 增加调度、平台早退、收藏转发和 GUI 互斥回归测试
- [x] 完成 87 项全量测试与 Bilibili 真实仅封面验证
- [x] 重新构建并实际启动最终 EXE
- **状态：** complete

## 关键问题
1. 项目当前通过何种接口抽象平台下载、进度回调和 GUI 任务？
2. 本机与打包环境是否已有可用的 `ffmpeg`，以及是否允许引入成熟解析依赖？
3. Bilibili 未登录状态下该测试视频能获得的实际最高画质是多少，是否需要登录态才能突破清晰度限制？
4. 如何复用独立 Bilibili 浏览器登录态，让普通账号/大会员账号自动获得各自权限内的最高画质？
5. YouTube 测试链接在公开访问条件下实际暴露哪些格式，最高质量是否需要额外运行时依赖？
6. 如何彻底移除脆弱的 Google 登录流程，同时不影响 Deno/EJS、分段下载和真实最高质量策略？
7. 当前 YouTube 最高视频流的耗时主要来自解析、顺序 Range 请求、音频下载还是 FFmpeg 合并？
8. 如何让 GUI 的速度档位真正作用于 YouTube 大文件传输，并在 CDN 不支持并行 Range 时安全回退？
9. 如何在不混入 Obsidian 仓库文件的前提下，仅提交 downloader 的源码、测试、文档和项目状态？
10. TikTok 单作品当前返回的是合并流还是分离流，如何避开较低优先级的带水印候选并稳定选择最高质量？
11. 如何在不收集账号密码、不依赖 Google 自动化登录窗口的情况下，让普通用户在软件内配置可选 YouTube 登录态？
12. Windows 上 Chrome/Edge 应用绑定加密和数据库锁定使自动读取不稳定时，如何提供可靠降级路径？
13. 当前 HTTP 429、机器人确认、PO Token 缺失与真正私享/年龄限制错误如何准确区分？
14. Chrome 窗口关闭后仍有后台应用或扩展持有 Cookie 数据库时，软件应如何让普通用户理解并安全退出？
15. 如何让 Bilibili 单视频真正使用速度档位并行下载，同时保留其备用 CDN 节点和串行稳定回退？
16. 如何让各平台封面都按实际可解码像素选择最高质量，同时避免重复下载、Cookie 跨域和封面失败误报媒体失败？
17. 如何在不引入沉重 GUI 依赖、不改变下载业务逻辑的前提下，让 Tkinter 主界面获得 Apple 风格的留白、层级和响应式布局？
18. 如何让用户选择父目录后统一创建 `下载结果/`，并在重启、打开目录和实际任务调度中保持一致？
19. 如何让“仅下载封面”复用现有最高质量候选链路，同时保证封面失败可见且任何媒体引擎都不会启动？
20. 如何把 CSS Design Awards、React Bits、Aceternity UI 与 Uiverse 的优秀层级和微交互转译为原生 Tkinter，而不照搬网页特效或破坏既有下载行为？

## 已做决策
| 决策 | 理由 |
|------|------|
| 长期规则写入 `AGENTS.md`，任务状态写入三个规划文件 | 避免把短期实现细节固化为项目规则 |
| 项目专属结论不写入跨项目经验总结 | 遵守用户指定的记忆边界 |
| Bilibili 作为独立平台模块接入，GUI 仅显示“视频媒体” | 保持现有架构边界并控制功能范围 |
| 最高画质使用分离流时必须通过 FFmpeg 无重编码合并 | 避免为了单直链降质，也避免产生无声成品 |
| 使用 `yt-dlp` 2026.07.04 或更高版本作为 Bilibili 解析依赖 | 复用成熟且持续维护的站点适配，避免绑定易变私有接口 |
| 下载到任务临时目录，FFprobe 验证音视频后再移动为成品 | 失败不留下被误认作完成品的文件 |
| 最高质量排序固定为分辨率优先，其次帧率、码率；不写死 1080p | 普通账号、未登录和大会员应由源站返回的可用格式决定上限 |
| Bilibili 使用独立浏览器 profile，Cookie 只在本机内存与忽略目录中使用 | 支持会员清晰度且避免与其他平台登录态混用或泄露 |
| `yt-dlp` 登录态使用域名受限 CookieJar，不再放进全局 `http_headers` | 消除新版弃用警告，并由 Cookie 作用域约束发送域名 |
| YouTube 仅支持公开可访问内容，不提供登录入口或账号 Cookie | Google 拒绝自动化浏览器登录，且测试链接未登录已验证可下载 4K50；不为非必要登录增加脆弱流程 |
| YouTube 普通 HTTP 流保留 4 MiB 块并按速度档位使用 2/4/8 路并行 Range；失败时重新解析并回退 yt-dlp 稳定分段 | 当前实测瓶颈是单连接吞吐；并行提升明显，同时不能牺牲对不支持 Range、签名过期和瞬时 403 的容错 |
| YouTube 阶段追加到现有融合下载器主经历，不另建重复经历 | 遵守“一项目一主文件”，保留完整演化链并避免索引分裂 |
| TikTok 首阶段只支持公开单作品链接，并按 `res → fps → br` 选择最高候选 | 控制功能范围；当前测试链接的最高无水印标记候选已同时包含 H.265 视频和 AAC 音频 |
| YouTube 保留匿名默认，并新增 Firefox、Chrome/Edge 尽力读取与 cookies.txt 导入 | 避免再次使用被 Google 拒绝的自动化登录窗口，也不强迫公开视频用户登录 |
| 不承诺自动读取 Windows Chrome/Edge Cookie成功 | Chromium 应用绑定加密与运行中数据库锁定使跨应用自动读取缺少可靠通用方案 |
| YouTube Cookie 不作为全局 HTTP Header 注入，登录配置与敏感文件必须留在忽略目录 | 降低跨域泄露、日志泄露和误提交风险 |
| 将“扩展导出 + 自动查找导入”作为 Chrome/Edge 普通用户的推荐路径，直接读取下沉为高级实验项 | Windows 浏览器数据库锁定和应用绑定加密不可稳定绕过；用户主动导出可验证、可解释且不需要软件接触账号密码 |
| Bilibili 沿用已验证的 4 MiB 分块，按速度档位使用最多 8 路并行 Range；每块校验 206、Content-Range、总大小和实际长度，失败后回退原串行路径 | 提升单视频吞吐，同时不降低画质、不接受损坏分块，也不丢失备用 CDN 自动轮换能力 |
| 封面使用跨平台统一开关，平台模块负责提取候选，共享逻辑按实际解码尺寸与字节数择优 | 保持 GUI 简洁和平台边界；不把 URL 名称或接口声明误当成最高质量证据 |
| 封面失败记录为作品级警告，不撤销已经验证成功的媒体产物 | 避免媒体已成功却被误报为完全失败，同时让封面缺失保持可见 |
| GUI 只保存用户选择的父目录，实际任务根目录始终由该父目录追加 `下载结果/` 得到 | 满足自定义位置需求，同时继续维持五个平台统一输出根目录 |
| “同时保存封面”与“仅下载封面”使用独立状态；仅封面模式在平台解析后、媒体引擎启动前返回 | 保留原任务兼容性，并为用户提供可验证的零媒体传输路径 |
| GUI 采用深绿色品牌横幅、浅色 Bento 工作区和单一绿色主行动，不引入 Web 动画框架 | 吸收参考站点的信息层级、卡片分组与微交互，同时符合 Tkinter 的性能、可维护性和桌面使用场景 |
| 应用图标使用面向 16–48px 的近景简化图，并在 ICO 中写入 9 个独立尺寸 | 复杂全身原图直接缩小会丢失轮廓；分尺寸资源能让 Windows 标题栏、任务栏和高分屏分别选择更合适的像素版本 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| PowerShell/Windows 下 `rg ... *.spec` 把通配符当作非法路径 | 1 | 后续由 PowerShell 先解析文件列表，或使用 `rg -g '*.spec'` |
| Bilibili URL 正则把中文逗号后的文字纳入查询参数，导致 1 项测试失败 | 1 | 在中文分享文案标点处显式截断 URL，并增加回归测试 |
| 真实下载命令的外层时限误设为 10 秒，任务在解析/下载前期被终止 | 1 | 保留代码内网络超时，外层改为长任务时限并用分段等待获取进度 |
| 第二次真实下载在所选 AV1 CDN 流上等待超过 3 分钟且文件为 0 字节 | 1 | 停止该任务，分别探测同清晰度 AV1/AVC 小范围响应后调整质量排序或网络参数 |
| 清理遗留进程与临时目录的组合命令被安全策略拒绝 | 1 | 分开执行温和进程终止；临时目录待确认无锁后再按已验证绝对路径清理 |
| 10 MiB Range 分段被 Bilibili CDN 以 Windows 10054 重置，3 次重试后失败 | 1 | 不重复该分段大小；探测更小分段后选择稳定上限 |
| 规划文件阶段更新补丁因上下文变化而校验失败 | 2 | 分文件读取并小范围合并，未覆盖现有内容或影响源码 |
| `check-complete.ps1` 未识别中文阶段格式并报告 0/0 | 5 | 本轮人工核对 14 个阶段均为 complete、未勾选项为 0；以测试、真实解析、GUI 和打包证据完成审计 |
| 提交前 `git diff --cached --check` 发现 `tests/__init__.py` 末尾多余空行 | 1 | 删除多余空行，重新暂存后再次执行空白检查 |
| YouTube 测试链接元数据可解析，但最高视频流正式下载返回 `HTTP 403` | 1 | 重新解析验证普通 GET/Range 后采用 4 MiB HTTP 分段；完整 4K50 下载成功 |
| 用 `pyi-archive_viewer -l` 搜索 `downloaders.youtube` 未命中，归档检查提前停止 | 1 | 不重复假设模块以明文条目存在；先检查归档实际命名，再结合 PyInstaller TOC 验证模块与捆绑二进制 |
| PowerShell 双引号中的 `rg` 正则被转义破坏并报 `unclosed group` | 1 | 改用单引号字面正则或 `Select-String -SimpleMatch`，不重复原命令 |
| YouTube 32 MiB 单 Range 基准在当前 CDN 节点 45 秒无响应并超时 | 1 | 不重复大块单连接；重新解析签名 URL，改用小 Range、逐策略捕获失败并比较并行请求 |
| 最终 PyInstaller 在覆盖 `dist/融合下载器.exe` 时返回 `WinError 5` | 1 | 先只读核对占用该精确 EXE 的进程；只终止本轮验证遗留进程或在确认后重试，不清理其他用户进程/文件 |
| 最终规划审计发现 `progress.md` 阶段 10 仍标为 `in_progress` | 1 | 将阶段日志状态同步为 complete 后重新执行无活动项检查 |
| TikTok 真实文件已下载成功，但标题中的 🙂 导致 PowerShell GBK 日志抛出 `UnicodeEncodeError` 并误报任务失败 | 1 | 将日志输出包装为编码失败时按实际控制台编码替换不可表示字符后重试，媒体文件名本身保持原始 Unicode |
| 当前开发机未安装 Firefox，推荐路径无法在本机做真实浏览器 Cookie 验证 | 1 | 保留 Firefox 推荐入口，增加 Chrome/Edge 现有状态尽力读取和 cookies.txt 备用；不擅自读取真实用户 Cookie |
| 首次 GUI 隐藏回归只在根窗口直接子控件中寻找教程窗口，因教程窗口挂在设置窗口下而触发 `StopIteration` | 1 | 改为递归遍历设置窗口子树定位 Toplevel，不重复错误的控件层级假设 |
| 打包后启动验证命令把 PowerShell `foreach` 语句直接接到管道，解析阶段报 `empty pipe element` | 1 | 先把循环输出赋给 `$rows` 再格式化；命令未进入执行阶段，因此没有遗留测试进程 |
| Bilibili 真实速度验证首次使用 1 秒外层命令时限，下载进程被工具提前终止 | 1 | 先确认无遗留进程和临时目录，再改用可持续等待的执行单元完成整段下载，不原样重复短时限 |
| 15:41:39 的正式 EXE 早于 15:44:50 的最终 Bilibili 并行源码 | 1 | 保留已验收的 YouTube 包记录，但必须在阶段 17 源码完成后重新构建，不能把旧包误称为包含 Bilibili 提速 |
| 系统临时目录中的真实封面验收脚本首次启动后无法导入项目 `downloaders` 包 | 1 | 在脚本启动时显式把项目根目录加入 `sys.path`；首次失败发生在导入阶段，没有发起网络请求或产生封面文件 |
| 独立图片核验命令再次把 PowerShell `foreach` 直接接到管道并报 `empty pipe element` | 1 | 先把循环结果赋给 `$rows` 再格式化；修正后五张图片均由 System.Drawing 成功解码 |
| 查询已经退出的临时 Chrome 进程时 `Get-CimInstance Win32_Process` 两次超时 | 2 | 停止依赖 CIM；改用精确 PID、DevTools 端口和 `Get-Process` 只读确认进程已退出 |
| 清理本轮临时文件时 `Remove-Item` 命令被安全策略拒绝 | 2 | 先逐项验证绝对路径均位于系统 TEMP，再在同一 PowerShell 进程用 .NET 文件 API 清理并确认 6 个目标均不存在 |
| 用 `pyi-archive_viewer -l` 只检查 EXE 外层归档时未看到 `downloaders.covers` | 1 | 不把外层资源列表误当完整模块清单；改查本次构建的 `PYZ-00.toc`，确认共享封面与五个平台模块均已打入 |

## 备注
- 外部网页或接口返回内容只记录到 `findings.md`，不写入本计划。
- 每完成一个阶段即更新状态；发生错误时记录后更换方法，不机械重试。

## 阶段 30：抖音收藏夹精确 ID 与手动安全更新（2026-08-01）
- [x] 复现用户目录下“收藏夹 HTTP 200 但 0 个作品”
- [x] 区分 Windows 残留代理、任务临时直连、登录态和接口数据层
- [x] 对照抖音网页确认收藏夹长整数 ID 被 JavaScript 舍入
- [x] 优先使用 `collects_id_str` 并真实读取“图片”收藏夹作品
- [x] 确认现有 GitHub Release、资产名、大小和 SHA-256 字段
- [x] 实现用户主动检查、两次确认、下载、大小与 SHA-256 校验
- [x] 实现独立替换助手、旧版备份、启动失败回滚和重新启动
- [x] 增加版本、收藏夹、更新下载与 GUI 回归测试
- [x] 运行全量单测、语法检查和真实 GUI 验收
- [x] 重新打包并验证正式 EXE 内含更新助手
- [x] 在隔离夹具完成实际 EXE 替换与回滚路径验收
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| 收藏夹解析优先采用 `collects_id_str` | 收藏夹 ID 超过 JavaScript 安全整数范围；数字字段末位被舍入会让合法请求静默返回空列表 |
| v2.2.0 作为首个带软件内更新能力的版本 | 当前 GitHub 正式版为 v2.1；旧版没有更新入口，因此只能从 v2.2.0 开始支持后续软件内更新 |
| 只检查正式 GitHub Release，不直接拉仓库源码 | Release 提供明确版本、更新说明、固定 EXE、大小和 SHA-256，适合普通用户且便于校验 |
| 用户确认下载和确认安装分开 | 检查更新是只读动作；大文件传输和关闭替换应用分别需要清楚授权 |
| 替换只处理当前 EXE并保留一个旧版备份 | 单文件便携应用的数据均在旁边目录；只替换 EXE可最大限度保护设置、登录态和下载结果 |

## 阶段 23：抖音图文正文 Markdown（2026-07-30）
- [x] 核对当前抖音页面、详情接口、内置浏览器、收藏夹与媒体跳过链路
- [x] 用用户提供的两个入口确认真实作品类型和正文来源
- [x] 从图文作品详情提取完整正文并生成稳定命名的 UTF-8 Markdown
- [x] 让已有图片跳过、普通单作品与收藏夹入口仍能补齐缺失 Markdown
- [x] 增加正文选择、Markdown 内容、重复运行与收藏跳过回归测试
- [x] 运行全量单测、语法检查和受影响调度回归
- [x] 在隔离目录用两个真实入口完成最终产物验收
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| Markdown 作为抖音普通“作品媒体”任务的自动附加产物，不增加额外 GUI 开关 | 用户期望图文下载天然包含图片和文章；单独开关会让原问题继续被静默遗漏 |
| 仅在作品含图片或存在抖音长文章结构且正文完整时生成 Markdown；“仅下载封面”保持原有零正文语义 | 避免把普通视频短标题批量变成无价值 Markdown，也覆盖没有传统图片数组的长文章 |
| 长文章优先读取 `article_info.article_content.markdown`，普通图文优先读取 `share_info.share_desc_info` | 两个真实样本分别使用不同正文结构，顶层 `desc` 都不足以代表完整内容 |
| Markdown 在媒体重复跳过判断前保存；相同内容复用，变化内容写入不覆盖的新文件 | 确保已有图片的历史任务能补齐文章，同时遵守同名文件不得静默覆盖 |

## 阶段 24：抖音长文章高清图片本地化（2026-07-30）
- [x] 核对用户 Typora 图片规则与当前长文章远程图片引用
- [x] 实测同一文章图片多个 CDN 候选的实际像素和字节
- [x] 下载每张文章图片的最高质量候选到 Markdown 同级 `assets/`
- [x] 将远程图片语法改写为 Typora 兼容的 `./assets/...` 相对路径
- [x] 增加候选选择、重复复用、失败回退和路径可解析测试
- [x] 用真实长文章核验全部图片、本地引用、清晰度与重复运行
- [x] 运行全量测试、重新打包并实际启动最终 EXE
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| 直接生成 `assets/` 并在 Markdown 中写 `./assets/...` | 与用户 Typora“复制图片到 ./assets 文件夹”、相对路径和 `./` 偏好完全一致，无需用户改设置 |
| 每张图实际下载并解码所有可用候选，再按像素面积和有效字节数选择 | 同一流程图实测 Markdown 预览候选仅 800×500，而最高候选达到 1171×732，不能依赖 URL 名称 |
| 单张图片失败时保留原远程引用并记录警告 | 避免一张 CDN 图片暂时失效导致整篇文章正文丢失或产生坏链接 |

## 阶段 25：底部输出控制与按博主分类（2026-07-30）
- [x] 核对顶部位置按钮、底部路径栏、配置持久化与五平台作者字段
- [x] 新增统一的 Windows 安全作者目录解析
- [x] 默认按博主分类，并提供旧版全部平铺模式
- [x] 将“更改位置 / 打开下载结果”移动到底部输出栏
- [x] 将保存方式贯穿单作品、批量、收藏、评论图片和封面链路
- [x] 增加路径、配置、GUI、五平台和调度回归测试
- [x] 运行全量测试、真实 GUI 验收、重新打包并启动最终 EXE
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| `TaskOptions` 统一携带 `organize_by_author`，各平台在解析到真实作者后选择最终目录 | 作者名只有解析作品详情后才可靠，不能由 GUI 根据链接猜测 |
| GUI 默认启用按博主分类，直接调用平台函数仍保留平铺默认 | 满足用户新默认，同时维持平台模块既有调用者的兼容性 |
| 临时下载仍放在下载根目录，验证完成品再移动到作者目录 | 避免在元数据尚未解析时创建错误作者目录，也保持失败临时文件与完成品边界 |
| 作者文件夹只使用作者名并做 Windows 安全化 | 符合用户预期；处理非法字符、保留名、空名称和尾随点空格 |

## 阶段 26：微信公众号与知乎文章 Markdown（2026-07-30）
- [x] 核对干净工作树、现有调度契约、抖音 Markdown/`assets` 规则和作者目录实现
- [x] 使用用户测试链接确认微信公众号公开页可直接读取完整正文，知乎普通 HTTP/API/新配置浏览器会被 403 拦截
- [x] 使用当前 Chrome/OpenCLI 登录会话确认知乎测试回答可精确定位到目标回答正文
- [x] 新增通用网页文章正文清洗、Markdown 转换、高清图片本地化和重复复用能力
- [x] 新增微信公众号文章解析与下载模块
- [x] 新增知乎回答/专栏解析、当前 Chrome 会话与软件专属登录态兜底模块
- [x] 接入 GUI、统一调度、按作者分类、知乎登录入口与 README
- [x] 增加链接解析、正文范围、图片候选、调度和 GUI 回归测试
- [x] 运行全项目测试、语法检查和差异检查
- [x] 使用两个真实链接完成隔离目录下载，核验 Markdown、作者目录、正文边界和本地图片
- [x] 重新构建并实际启动正式 EXE
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| 微信公众号直接请求并只转换 `#js_content` | 测试页公开可访问，正文边界稳定；避免混入页面导航、推荐和二维码外壳 |
| 微信正文图片先按声明尺寸过滤，再以实际解码尺寸过滤小于 120px 的装饰图 | 测试页 50 个图片节点中多数是 24/64px 图标，真正正文配图只有 3 张 |
| 知乎按公开页面 → 当前 Chrome/OpenCLI → 软件专属登录态顺序读取 | 当前网络下普通页面/API/新配置浏览器均 403，而当前正常 Chrome 会话能精确读取目标回答 |
| 知乎回答必须按回答 ID 定位 `.AnswerItem` | 问题页包含多个回答，整页转换会把其他作者、回答和导航误写入 Markdown |
| 两个平台共用文章本地化核心和 `./assets/...` 规则 | 统一实际像素择优、失败保留远程引用、作者分类与重复复用行为，并适配用户 Typora 设置 |

## 阶段 27：OpenCLI 下载窗口自动清理（2026-07-30）
- [x] 复现知乎下载完成后 OpenCLI 窗口退回 `about:blank` 但不退出
- [x] 核对 OpenCLI 当前 `browser close` 的真实租约释放语义
- [x] 审计其他平台的浏览器启动、复用和退出边界
- [x] 新增只识别并关闭本次 OpenCLI 任务窗口的 Windows 安全清理器
- [x] 将清理器接入知乎正文和抖音评论两条 OpenCLI 路径
- [x] 增加用户窗口保护、歧义失败关闭和成功/异常释放回归测试
- [x] 运行全项目测试、语法检查和差异检查
- [x] 使用真实知乎链接确认正文提取成功、OpenCLI 窗口退出且现有 Chrome 窗口不变
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| 继续先调用 OpenCLI `browser close`，再对精确识别且已恢复 `about:blank` 的原生窗口发送 `WM_CLOSE` | 保留 OpenCLI 自身会话释放流程，同时补齐其不关闭窗口外壳的行为 |
| 只认“本轮新窗口”或“打开前为 `about:blank`、打开后变为正文页”的 Chrome/Edge 窗口 | 防止关闭用户已有正常页面、翻译窗口或并行打开的新窗口 |
| 候选多于一个或释放后不再是 `about:blank` 时放弃关闭 | 宁可保留一个空白窗口，也不以猜测方式影响正常浏览器使用 |
| 登录按钮打开的专属浏览器继续由用户主动关闭 | 登录是人工流程，不应在后台下载任务结束时误关用户尚在操作的窗口 |

## 阶段 28：微信贴图文字与最高质量原图（2026-07-31）
- [x] 用给定视频号和贴图链接核对公开网页、动态接口与 Chrome 实际页面结构
- [x] 判断视频号最高质量下载需要电脑微信、代理抓包、根证书和流解密，按用户条件收敛为只实现贴图
- [x] 扩展微信 URL/模板解析，贴图正文与作品画廊合并进入现有 Markdown/`assets/` 链路
- [x] 普通请求触发访问验证时自动回退当前 Chrome/OpenCLI，并等待正文、作者与画廊稳定
- [x] 清理 qpic 临时/水印参数候选，按实际解码结果选择最高质量图片
- [x] 增加 URL、模板、验证码回退、浏览器清理、原图候选和重复复用测试
- [x] 用真实贴图链接完成统一调度、3 张原图、作者目录、重复复用与窗口清理验证
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| 本阶段只实现微信贴图，不实现视频号流解密 | 用户指定“难则只实现贴图”；视频号真实流需要高侵入的代理、证书和电脑微信协议适配 |
| 贴图继续复用文章 Markdown 与 `assets/` 核心 | 保持作者分类、图片实际解码、失败隔离和重复复用一致 |
| 浏览器快照等待正文、作者和去重画廊数量连续稳定 3 秒 | 真实页面会先渲染首图，立即提取会漏图和丢作者 |
| canonical URL 只剔除临时 `poc_token` | 保留作品身份字段，同时让相同贴图重复运行可复用同一 Markdown |

## 阶段 29：微信视频号原始质量下载（2026-07-31）
- [x] 审计 `res-downloader` 与 `wx_channels_download` 的代理、分享解析、清晰度与解密实现
- [x] 确认参考项目的公开分享解析服务可为测试短链返回 H.264/H.265 媒体候选
- [x] 验证只保留 `encfilekey` 与 `token` 的原始地址无需本机代理或证书即可访问
- [x] 新增视频号短链/预览链接提取、公开解析、原始质量选择、候选探测与失败回退
- [x] 接入统一微信入口、批量失败聚合、作者目录、重复复用和 GUI 自动识别模式
- [x] 使用有界并行 Range 下载，失败时回退稳定单连接，并通过 FFprobe 验证音视频轨
- [x] 增加 URL、解析响应、原始候选、质量回退、下载复用、调度与 GUI 回归测试
- [x] 使用真实测试链接完成统一调度下载与重复跳过验证，临时媒体验收后自动清理
- [x] 运行全项目测试、更新正式 EXE 并提交改动
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| 使用参考项目披露的公开分享解析服务，不接入本机 MITM | 测试链接可直接获得短期媒体候选；避免安装根证书、修改系统代理和依赖电脑微信版本 |
| 原始候选只保留 `encfilekey` 与 `token` | 参考实现将其定义为“原始视频”；真实样本由 576p 在线版提升为 1080p 原始版 |
| 原始候选可访问时优先，失效后才按可访问文件大小回退在线候选 | 保持最高质量目标，同时为短期签名失效提供可理解的降级路径 |
| 第三方服务只接收用户粘贴的公开分享链接 | 不发送 Cookie、微信会话或浏览器数据；短期媒体 URL 不记录、不持久化 |
| 最终 MP4 必须通过 FFprobe 同时验证视频和音频 | HTTP 200 与文件大小正确不等于可播放完成品 |

## 阶段 30：微信贴图上传原文件与无水印优先（2026-07-31）
- [x] 复现新测试链接得到 2560×3840 水印展示图
- [x] 对照页面脚本确认每张贴图分别提供 `original_info`、`cdn_url` 与 `watermark_info`
- [x] 新增页面脚本记录解析、JS 转义解码和短链接贴图识别
- [x] 按上传原文件 → 无水印 CDN 图 → 水印兜底图构造候选
- [x] 将 OpenCLI 回传改为等待正文后读取精简结构，避免 2.5 MB 整页 DOM 超时
- [x] 增加上传原文件、无水印/水印分层、公开页面壳补全和窗口释放测试
- [x] 用真实链接完成 9 张上传原文件的隔离下载与逐张解码验证
- [x] 运行全项目测试、重新构建正式 EXE 并完成启动验收
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| `original_info` 可解码时优先于 2560×3840 的 `cdn_url` | 用户样本的上传原文件最高达到 5120×7680/46,308,272 字节，网页展示图并非最高质量 |
| `watermark_info` 始终标记为水印候选，只作最后兜底 | 该水印已写入独立 CDN 文件的像素，删除 URL 查询参数无法去除 |
| 公开短链接直接从页面脚本恢复贴图正文与 9 张记录 | 页面脚本已包含标题、说明、作者、原文件和水印分层，无需为公开页面启动浏览器 |
| 浏览器兜底只返回正文、作者、标题和当前画廊节点 | 避免回传约 2.5 MB `documentElement.outerHTML` 导致 OpenCLI 超时 |

## 阶段 31：抖音/小红书既有作品最高质量复核（2026-07-31）
- [x] 对用户已有抖音 24 张图和小红书 3 张图 + 3 段 Live Photo 建立实图/FFprobe 基线
- [x] 通过抖音真实 Chrome 详情接口枚举图集全部候选，并逐张与旧文件做 SHA-256 对照
- [x] 实测抖音原对象、q95、q100 变体的可访问边界
- [x] 枚举并实际下载小红书每张图片的 raw、PNG、WebP 与预览候选
- [x] 识别此前被 Pillow 拒绝的 `image/heic` 原始对象及主图/HDR 辅助网格
- [x] 实现 HEIC/HEIF/AVIF 容器识别、主图尺寸解析和原始容器优先
- [x] 将作品级“有任意旧文件即跳过”改为重新探测后逐文件精确复用
- [x] 增加 HEIC 解析、候选穷举和无重复复用测试
- [x] 使用真实小红书链接完成隔离目录首次下载与第二次复用验证
- [x] 运行全项目测试、语法检查和差异检查
- [x] 重新构建并实际启动正式 EXE
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| 抖音维持现有候选选择，不伪造 q95/q100 原图地址 | 24 张当前最佳候选与旧文件逐张 SHA-256 一致；源尺寸只到现有 2160×…，修改签名路径均被 CDN 拒绝 |
| 小红书优先保存全尺寸 HEIC/HEIF/AVIF 原始容器 | 测试 raw 响应为 `image/heic`，包含 3024×4032 主图和 HDR 辅助网格；服务端 PNG 像素相同但已转码且不保留原容器 |
| 只对全尺寸 HEIF-family raw 使用可靠的提前收敛 | 该候选同时满足原始对象、容器可验证和页面声明主图面积；普通 JPEG/PNG/WebP 仍全部实际解码，避免再次被首个“看起来够大”的候选误导 |
| 取消作品/收藏级旧文件快跳过，改为逐文件比较后复用 | 旧 PNG 不应阻止新增 HEIC；同一 HEIC 按完整字节比较复用，视频按当前最佳候选长度复用，避免生成 `_1` 副本 |

## 阶段 32：失效系统代理直连回退与任务终止（2026-07-31）
- [x] 复现 Windows 手动代理残留导致 `requests` 访问 `127.0.0.1:10808` 被拒绝
- [x] 确认故障来自 Requests 继承系统代理，与 IDM 下载引擎无关
- [x] 对不可达的本机代理实施任务级临时直连回退，任务后恢复环境
- [x] 将主操作按钮在运行时切换为协作式终止入口
- [x] 在批量调度、候选探测、并发图片和流式下载中传播取消信号
- [x] 增加代理边界、取消传播、GUI 状态和临时文件清理测试
- [x] 使用用户抖音样本完成关闭代理条件下的真实下载与终止验收
- [x] 运行全项目测试、语法检查、正式 EXE 构建与窗口验收
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| 只在实际生效的代理指向本机且端口不可达时自动直连 | 保留可用代理和远程/企业代理行为，不把其他网络错误误判成代理故障 |
| 直连回退只影响当前任务并在结束后恢复进程环境 | 不修改 Windows 设置，也不替用户永久改变网络策略 |
| 采用协作式取消而非强制杀线程/进程 | Python 线程无法安全强杀；在网络请求返回后停止后续探测和写入，可避免损坏完成品 |

## 阶段 35：首页横幅极光光环动效（2026-08-06）
- [x] 在首页横幅右侧加入受参考图启发的原生 Canvas 极光光环动效。
- [x] 保持文字区静止、清晰，且不引入 WebGL、浏览器控件或外部素材。
- [x] 在隐藏/最小化和销毁时停止刷新计时器，避免后台资源消耗。
- [x] 完成自动化测试、语法检查、空白检查和真实窗口视觉验收。
- **状态：** complete

### 本阶段决策
| 决策 | 理由 |
|------|------|
| 使用三层 Canvas 弧线加三颗轨道粒子 | 保留低调科技感，适配既有 Windows Tkinter 运行时和打包边界 |
| 以 55 ms 间隔刷新，并在非可见状态停止 | 约 18 FPS 足以让小面积装饰平滑运动，同时不干扰下载、滚动和界面响应 |

## 阶段 33：小红书收藏登录态与页面入口修复（2026-08-05）
- [x] 复现小红书账号接口返回 406 / `code=-1`，确认旧 GUI 会将访客 Cookie 误报为账号已登录。
- [x] 通过已登录网页的“我/个人主页”入口定位当前账号，再读取“收藏-笔记”；接口失败仅降级，不再直接判未登录。
- [x] 增加登录态和收藏脚本回归用例，完成全量测试、语法检查、EXE 打包与启动验收。
- **状态：** complete

## 阶段 34：v2.3 发布资产内部版本同步（2026-08-06）
- [x] 确认线上 v2.3 Release 的 EXE 内部版本仍为 2.2.0，属于发布资产未在打包前同步版本号，不是更新检查逻辑错误。
- [x] 将 `app_version.py` 升级为 2.3.0，完成全量测试、语法检查和重新打包。
- [x] 将用户实际使用的 `dist/严选/Fusion Downloader.exe` 替换为新构建，并保留可回退旧版。
- [ ] 将 GitHub v2.3 Release 的 `Fusion.Downloader.exe` 替换为本次重新打包的资产，并复核线上 SHA-256 后完成公开更新链路验收。
- **状态：** local complete; release asset pending

## 阶段 36：首页横幅全幅视觉与 EXE 打包（2026-08-06）
- [x] 以原创黑洞视觉替换低保真 Canvas 光环，并改为全幅展示。
- [x] 移除横幅内的英文标签、主标题和说明文案，仅保留应用页面自身身份信息。
- [x] 将 PNG 资源加入 PyInstaller 数据清单，并补充资源存在性与动效回归测试。
- [x] 重新构建正式 EXE，确认内置资源、版本与窗口启动。
- **状态：** complete
